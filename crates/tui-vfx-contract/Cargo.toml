# <FILE>crates/tui-vfx-contract/Cargo.toml</FILE> - <DESC>v3.1 public contract DTO crate manifest</DESC>
# <VERS>VERSION: 0.2.0</VERS>
# <WCTX>Authoring shorthand canonicalize: serde_json is now a runtime dependency for the canonicalize entry point.</WCTX>
# <CLOG>0.2.0: MINOR — promote serde_json from dev-deps to runtime deps for canonicalize_recipe(serde_json::Value) public API.
# 0.1.0: INIT — add contract crate with foundational tui-vfx-types, serde, and schemars dependencies.</CLOG>

[package]
name = "tui-vfx-contract"
description = "Stable v3.1 contract DTOs for tui-vfx"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
tui-vfx-types = { workspace = true, features = ["schemars"] }
serde.workspace = true
serde_json.workspace = true
schemars.workspace = true

# <FILE>crates/tui-vfx-contract/Cargo.toml</FILE> - <DESC>v3.1 public contract DTO crate manifest</DESC>
# <VERS>END OF VERSION: 0.2.0</VERS>
