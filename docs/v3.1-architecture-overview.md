<!-- <FILE>docs/v3.1-architecture-overview.md</FILE> - <DESC>v3.1 clean-room kernel architecture and schema-reference overview</DESC> -->
<!-- <VERS>VERSION: 0.18.0</VERS> -->
<!-- <WCTX>New kernel Phase K0: record primitive migration, descriptor catalog, and skeleton player layers.</WCTX> -->
<!-- <CLOG>0.18.0: MINOR — record Phase K0 contract-native skeleton player boundary.
0.17.0: MINOR — record Phase J2 shared primitive descriptor pack/catalog layer.
0.16.0: MINOR — record Phase J0/J1 migration pilot and validator harness layers.
0.15.0: MINOR — record Phase I0 lifecycle/time/trigger contract layer.
0.14.0: MINOR — record Phase H1 canonical recipe document, vocabulary, and evidence notes.
0.13.0: MINOR — record Phase H0 source, asset, and procedural source contracts.
0.12.0: MINOR — record Phase G4 graph value bus and node output semantics.
0.11.0: MINOR — record Phase G3 graph topology and channel-aware merge semantics.
0.10.0: MINOR — record Phase G2 canonical graph execution proof.
0.9.0: MINOR — record Phase G1 canonical graph container.
0.8.0: MINOR — record Phase F2 declarative value sources, parameters, signals, and parameter bindings.
0.7.0: MINOR — record Phase F1 typed value/input descriptor contract.
0.6.0: MINOR — record minimal EffectDescriptor capability contract and Phase F input deferral.
0.5.0: MINOR — record physical contract/proof crate split before descriptor model.
0.4.0: MINOR — add Phase D3 contract/engine boundary and refresh roadmap.
0.3.0: MINOR — add Phase D2 template composition design and updated roadmap.
0.2.0: MINOR — add Phase D1 scene composition semantics and updated roadmap.
0.1.0: INIT — document the contract-first philosophy, progressive phase stack, and Rust/rustdoc/schema reference path.</CLOG> -->

# v3.1 Architecture Overview

## Status

This is the durable overview for the clean-room `tui-vfx-contract` / `tui-vfx-next` kernel stack. It describes the contract stack being proven before recipe, runtime, studio, or migration work begins. It is not a legacy-compositor migration plan.

## Philosophy

v3.1 is a contract-first rendering model.

The goal is not to preserve the old implementation shape. The goal is to make every public semantic rule explicit, testable, documented, and discoverable by tools.

A render effect should not hide what it reads, writes, targets, emits, or mutates. Recipes, studio controls, validation, diagnostics, and runtime behavior should all flow from the same Rust-owned contract model.

The legacy engine is an oracle and inventory. It is not the source of truth for v3.1 semantics.

Short prompt form:

```text
Build the contract before porting effects. Every semantic fact needs one owner, one test, and one generated reference path.
```

## Reference path

```text
Serde
    owns JSON wire shape

Schemars
    generates JSON Schema from Rust types

Rustdoc comments
    become human-facing schema descriptions/reference text

Generated schema/reference docs
    become the public contract artifact
```

Rust contract types are the schema source of truth. Hand-written contract docs explain the system; they are not the authoritative machine-readable schema.

## Progressive contract stack

```text
+==================================================================================================+
|                                 TUI-VFX v3.1 CLEAN-ROOM KERNEL                                    |
|                           Progressive contract stack, extended by phase                           |
+==================================================================================================+

  PHASE A — SURFACE

        Semantic Surface
          cells: dense rectangular grid
          roles: one role per cell
          metadata
              |
              v
        Surface Writes
          write cell
          copy sampled-source role
          preserve destination role
          set explicit role
          skip transparent empty
              |
              v
        Diagnostics
          zero-cell scope
          size mismatch

  PHASE B — SAMPLED SOURCE

        Destination coordinate
              |
              v
        CoordinateSampler
          identity
          shift
              |
              v
        Sampled source coordinate/cell/role
              |
              v
        Scope evaluation
          geometry: destination-local by default
          role: sampled-source by default

  PHASE C — ORDERED PIPELINE

        Current Surface
              |
              v
        Stage N reads current
        Stage N writes cloned next
              |
              v
        Next becomes current
              |
              v
        Later stages see prior cell and role writes

  PHASE D0 — SCHEMA / REFERENCE HYGIENE

        Rust contract types
          serde shape
          rustdoc comments
          JsonSchema derive
              |
              v
        Generated JSON Schemas
              |
              v
        Generated Schema Reference
          type docs
          field docs
          examples and validation notes

  PHASE D1 — SCENE / ELEMENT / LAYER COMPOSITION

        Scene
          final width / height
          declaration-ordered elements
              |
              v
        SceneElement
          stable element id
          optional layer id
          zIndex + declaration tie-break
          element-local surface
          placement in scene coordinates
              |
              v
        Final composed Surface
          higher/later writes overwrite lower/current cells
          skipped top cells preserve lower/current output
          diagnostics identify element id

  PHASE D2 — TEMPLATE COMPOSITION DESIGN

        Authoring Inputs
          templates define structure and slots
          mixins add reusable fragments
          presets/profiles supply values only
              |
              v
        Deterministic Expansion
          namespace ids
          resolve slots
          reject implicit conflicts
          preserve provenance
              |
              v
        Canonical v3.1 Recipe
          no template refs
          no inheritance
          no legacy aliases
              |
              v
        Future validation / compiler / runtime

  PHASE D3 — CONTRACT / ENGINE BOUNDARY

        Contract Vocabulary
          surface / scene / scope / write / sampler / diagnostics
              |
              v
        Proof Implementation
          surface engine
          proof pipeline
          toy stages and effects
              |
              v
        Schema Roots
          checked public artifacts
          proof roots clearly marked
              |
              v
        Future descriptors reuse vocabulary, not toy internals

  PHASE E0 — PHYSICAL CONTRACT SPLIT

        tui-vfx-contract
          stable DTOs
          stable schema roots
          no proof engine dependency
              |
              v
        tui-vfx-next
          proof engine
          toy stages
          proof-pipeline schemas
              |
              v
        Descriptors can land in contract crate

  PHASE E1 — MINIMAL EFFECT DESCRIPTOR

        EffectDescriptor
          identity, domain, cell access, scope support, write support, lifecycle
              |
              v
  PHASE F1 — TYPED INPUT DECLARATIONS

        ValueKind / Value / ValueSpec / EffectInputSpec
          defaults, ranges, enum domains, bindable hints, runtime mutability
              |
              v
  PHASE F2 — DECLARATIVE VALUE SOURCES AND PARAMETER BINDINGS

        ValueSource / ParameterSpec / SignalSpec / BindingSpec
          literal, parameter, signal, map, parameter-target replace bindings
          no runtime stores, node graph, recipe compiler, or studio manifest
              |
              v
  PHASE G1-G4 — CANONICAL GRAPH + VALUE BUS

        GraphSpec / NodeSpec / GraphStep / graph-local values
          descriptor-backed nodes, topology, parallel merge, node outputs
              |
              v
  PHASE H0 — SOURCE / ASSET / PROCEDURAL SOURCE CONTRACT

        SourceDescriptor / SourceSpec / SourceInputSpec
        AssetSpec / AssetRequirement / AssetRef
          sources produce semantic surfaces before effects transform them
          structured asset refs replace interpolation tokens
          source-local pipelines remain future integration points
              |
              v
  PHASE H1 — CANONICAL RECIPE DOCUMENT

        RecipeDocument / RecipeMetadata / RecipeScene
        SourceInstanceId / RecipeSceneElement / RecipeElementPipeline
          packages graph, descriptors, assets, sources, and scenes
          validates cross-document references without legacy aliases
          vocabulary and evidence notes guide future mapping pressure
              |
              v
  PHASE I0 — TIME / LIFECYCLE / TRIGGER CONTRACT

        ClockSpec / LifecycleSpec / PhaseSpec
        DwellPolicy / TriggerSpec / ValuePredicate
          attaches optional recipe lifecycle to RecipeDocument
          separates triggers from gates, bindings, loopbacks, and schedules
          validates typed predicates, latch/reset, and maxDuration caps
              |
              v
        Recipe v3.1 Compiler
              |
              v
        Runtime / Studio / Demos
```

## Phase D0 boundaries

Phase D0 backfills schema/reference hygiene only. It does not change runtime behavior and does not add real effects, recipes, studio manifests, runtime bindings, phase graphs, trigger engines, descriptor expansion, or legacy migration.

## Phase D1 boundaries

Phase D1 adds only scene / element / layer composition semantics. It composes multiple local semantic surfaces into one final `Surface`; it does not add effect descriptors, recipe schemas, studio manifests, runtime bindings, phase graphs, trigger engines, legacy migration, template inheritance, a full layer graph, complex blending, or real effect ports.

## Phase D2 boundaries

Phase D2 is a design-document phase for template composition. It defines compile-time expansion rules for templates, mixins/traits, presets, profiles, and recipes. It does not implement template expansion, recipe schema/compiler code, runtime inheritance, descriptors, studio manifests, phase graphs, trigger engines, legacy migration, or real effect ports.


## Phase D3 boundaries

Phase D3 is a boundary and vocabulary phase. It classifies which current `tui-vfx-next` types are public contract vocabulary, proof implementation, schema roots, or test-only scaffolding. It adds a logical contract/proof/schema-root grouping in `tui-vfx-next` and documents the boundary in [`v3.1-contract-boundary.md`](v3.1-contract-boundary.md).

Phase D3 does not implement effect descriptors, recipe schemas, source authoring schemas, template expansion, runtime bindings, phase graphs, trigger engines, studio manifests, legacy migration, real effect ports, or complex blend/layer behavior.


## Phase E0 boundaries

Phase E0 makes the D3 logical boundary physical. `tui-vfx-contract` owns stable surface, scene, scope, write, sampler, diagnostic, id, and outcome DTOs plus stable schema generation. `tui-vfx-next` depends on `tui-vfx-contract` and keeps proof execution, toy stages/effects, proof-pipeline schemas, and semantic engine tests.

Phase E0 does not implement effect descriptors, recipe schemas, source authoring schemas, template expansion, runtime bindings, phase graphs, trigger engines, studio manifests, legacy migration, real effect ports, or complex blend/layer behavior.

## Phase E1 boundaries

Phase E1 adds a minimal schema-backed `EffectDescriptor` model to `tui-vfx-contract`. The descriptor declares identity, broad `EffectDomain`, `CellAccess`, `ScopeSupport`, `WriteSupport`, and `EffectLifecycle`. Validation checks can accept or reject requested scope kinds, role write policies, cell write policies, and write channels against those declared capabilities.

Phase E1 does not implement typed effect inputs.

## Phase F1 boundaries

Phase F1 adds the first typed input contract to `tui-vfx-contract`: a closed `ValueKind` vocabulary, tagged literal `Value`, `ValueSpec` defaults/ranges/enum domains, `EffectInputId`, `EffectInputSpec`, `RuntimeMutability`, bindable metadata, and `EffectDescriptor.inputs`. Descriptor input validation rejects type mismatches, invalid ranges, enum-domain errors, non-finite numbers, and invalid input ids.

Phase F1 does not implement `ValueSource`, parameters, signals, bindings, presets, recipe nodes, descriptor registries, runtime graphs, studio controls, legacy migration, real effect ports, or template expansion.

## Phase F2 boundaries

Phase F2 adds the declarative source layer to `tui-vfx-contract`: `ValueSource`, `ParameterId`, `ParameterSpec`, `SignalId`, `SignalSpec`, `BindingTarget`, `BindingMode`, and parameter-target `BindingSpec`. Validation can resolve parameter/signal references, check source/target kind compatibility, validate fallbacks, constrain numeric maps, and reject unknown parameter binding targets.

Phase F2 does not implement runtime parameter/signal stores, live override precedence execution, node graph identity, direct effect-input bindings, recipe schema/compiler, template expansion, studio manifests/controls, phase graphs, trigger engines, migration, or real effect ports.

## Phase G4 boundaries

Phase G4 adds the graph-local node I/O value bus. `EffectDescriptor.outputs` declares descriptor-local effect outputs, `NodeSpec.outputs` publishes graph values either from an effect output or by re-emitting a resolved node input, and `ValueSource::GraphValue` lets later node inputs consume a graph-local value. Graph values have a typed kind and an explicit shape, including `frameValue` and `cellField`, so scalar-producing nodes can model spatial scalar fields such as normalized-x samples without collapsing them into one global number.

`tui-vfx-next` proves sequence visibility, fan-out, input re-emission, parallel branch isolation, post-join visibility, child-order last-writer-wins value conflicts, error-on-conflict value policy, and field-driven dimming. G4 still does not implement runtime parameter/signal stores, F2 binding execution, direct node/effect-input binding targets, recipe source syntax/compiler, phases/triggers/dwell, visibility predicates, loopback signals, asset/procedural sources, studio manifests, legacy migration, or real effect ports.

## Phase H0 boundaries

Phase H0 adds stable source and asset contracts to `tui-vfx-contract`. A `SourceDescriptor` describes a surface producer rather than an effect over an existing surface. A `SourceSpec` instantiates that descriptor with typed inputs and structural asset refs. `SourceInputSpec` reuses the existing `ValueSpec`, `Value`, `ValueKind`, and `ValueSource` vocabulary so effect inputs, source inputs, parameters, signals, and graph values do not fork the value model.

`AssetSpec`, `AssetRequirement`, and `AssetRef` make assets explicit contract objects. Canonical asset refs are structured references by id, not string interpolation tokens such as `{{ flag_art }}`. H0 accounts for text, card, procedural, image, ANSI, command-capture, and asset-backed source needs from recipe examples without adopting current recipe field names as canonical schema. H0 still does not implement canonical recipe documents, recipe lowering/compiler, source-local pipeline syntax, real asset loading/resolution, real procedural rendering, runtime stores, phase/trigger/dwell engines, studio manifests, migration, or real source/effect ports.

## Phase H1 boundaries

Phase H1 adds the canonical recipe document root to `tui-vfx-contract`. `RecipeDocument` packages metadata, assets, source descriptors, source instances, the canonical graph, and recipe scenes into one strict post-authoring/lowering contract. `RecipeSceneElement` references a source instance that will produce the element surface, and `RecipeElementPipeline` provides an element-local graph/topology reference seam for future source-local pipeline integration.

H1 also adds [`VOCABULARY.md`](VOCABULARY.md) as the standing human contract vocabulary and `docs/new_kernel/H1_RECIPE_EVIDENCE_NOTES.md` as curated evidence that representative legacy recipes map by concept rather than by field name. H1 still does not implement template expansion, legacy migration, runtime stores, binding execution, phase/trigger/dwell engines, studio manifests, asset loading, procedural rendering, or real source/effect ports.


## Phase I0 boundaries

Phase I0 adds recipe-level time, lifecycle, dwell, and trigger contracts to `tui-vfx-contract`. `ClockSpec` defines the lifecycle time sample space. `LifecycleSpec` owns ordered enter/dwell/exit phases. `DwellPolicy` can be fixed or trigger-terminated with an optional `maxDuration` cap. `TriggerSpec` owns condition, latch, reset, and action semantics using `ValueSource` plus typed `ValuePredicate`. `RecipeDocument.lifecycle: Option<LifecycleSpec>` attaches this contract to the canonical recipe root.

I0 also extends [`VOCABULARY.md`](VOCABULARY.md) and adds `docs/new_kernel/I0_EVENT_DWELL_EVIDENCE_NOTES.md` to lock the distinctions between clock, lifecycle, phase, trigger, gate, binding/value source, loopback, and effect-local schedule. I0 still does not implement a runtime player, trigger engine, parameter/signal stores, binding execution, loopback execution, migration/lowering, scene visibility gate execution, phase graph execution, effect-local schedule unification, source authoring syntax, or real ports.

## Phase J0/J1/J2 boundaries

Phase J0 proves a primitive migration pilot: old debug recipe evidence can be lowered into canonical v3.1 `RecipeDocument` fixtures, validated by `tui-vfx-contract-cli`, and stored under `/usr/projects/tui-vfx-recipes/recipes/v3.1/debug_recipes/` without mutating old recipe evidence.

Phase J1 hardens the validator harness with recursive directory validation, stable JSON reports, negative diagnostics, and descriptor-pack-aware validation. Phase J2 moves shared primitive descriptors into `descriptors/v3.1/packs/primitive.json` so second-ring migrated fixtures can reference a catalog instead of embedding standard descriptors in every recipe.

J0-J2 prove structural contract validity and migration harness behavior. They still do not prove visual parity or execute recipes as a player.

## Phase K0 boundaries

Phase K0 adds the first contract-native skeleton player. `tui-vfx-player` consumes canonical `RecipeDocument` values directly, validates against a loaded descriptor catalog, renders one sampled text-grid frame for the supported primitive subset, computes a deterministic render hash, and emits `v3.1.player.frame.1` reports. `tui-vfx-player-cli` provides `render-recipe` for one file or recursive smoke runs.

K0 supports `source.card`, optional `source.text`, `filter.dim`, `filter.tint`, `filter.invert`, `filter.greyscale`, `mask.none`, `mask.wipe`, `mask.checkers`, `sampler.sineWave`, basic enter/dwell/exit sampling, and stateful trigger-terminated dwell latching through `PlayerSession`.

K0 explicitly reports missing runtime adapters with structured unsupported diagnostics. It does not depend on `/usr/projects/tui-vfx-recipes/src`, does not load old recipes, does not implement template expansion, visual parity, full runtime stores, full procedurals, image/ANSI sources, studio controls, live playback, or real effect ports.

## Roadmap

```text
[DONE]    Phase A  — Semantic surface contract
[DONE]    Phase B  — Sampled-source semantics
[DONE]    Phase C  — Ordered pipeline semantics
[DONE]    Phase D0 — Schema/reference backfill
[DONE]    Phase D1 — Scene / element / layer composition semantics
[DONE]    Phase D2 — Template composition design document
[DONE]    Phase D3 — Contract/engine boundary and generalized scope/write vocabulary
[DONE]    Phase E0 — Physical contract split
[DONE]    Phase E1 — Minimal effect descriptor model
[DONE]    Phase F1 — ValueKind / Value / EffectInputSpec
[DONE]    Phase F2 — ValueSource / ParameterSpec / SignalSpec / BindingSpec
[DONE]    Phase G1 — Canonical Node Graph Container
[DONE]    Phase G2 — Canonical Graph Execution Proof
[DONE]    Phase G3 — Topology / parallel snapshot / channel-aware merge semantics
[DONE]    Phase G4 — Node I/O / hint value bus
[DONE]    Phase H0 — Source / asset / procedural source contract
[DONE]    Phase H1 — Canonical recipe document schema
[DONE]    Phase I0 — Time / lifecycle / trigger contract
[DONE]    Phase J0 — Primitive recipe migration pilot
[DONE]    Phase J1 — Validator hardening and fixture regression harness
[DONE]    Phase J2 — Shared primitive descriptor catalog and second-ring fixtures
[CURRENT] Phase K0 — Contract-native skeleton player
[LATER]   Phase K1+ — Player expansion, descriptor catalog strategy, recipe compiler/lowering, studio, ports
```

<!-- <FILE>docs/v3.1-architecture-overview.md</FILE> - <DESC>v3.1 clean-room kernel architecture and schema-reference overview</DESC> -->
<!-- <VERS>END OF VERSION: 0.18.0</VERS> -->
