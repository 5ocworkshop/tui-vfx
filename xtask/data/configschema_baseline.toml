# <FILE>xtask/data/configschema_baseline.toml</FILE> - <DESC>Baseline allowlist of pre-existing hand-written impl ConfigSchema for X blocks. EMPTY as of US-010 close-out; per docs/CONFIGSCHEMA_JUSTIFICATION.md the file is kept around schema_version=1 with no entries so the gate continues to load it without complaint, even though every hand-written impl in the workspace now carries a source-side CONFIGSCHEMA-JUSTIFICATION comment.</DESC>
# <VERS>VERSION: 2.0.0</VERS>
# <WCTX>Packet 1.9.A.followup US-008: drain the last 3 entries (StyleRegion, PathType, RoleTag) — all lifted into source comments with intentional-divergence-from-derive-output justifications. The grandfathering window opened by 1.9.A is now closed; new hand-written impls must carry a CONFIGSCHEMA-JUSTIFICATION line at the source site.</WCTX>
# <CLOG>2.0.0: MAJOR — drain the last 3 entries. Baseline is now empty. The gate enforces source-side justification for every hand-written impl in the workspace; the grandfathering door is closed. (US-010 may delete this file entirely per the empty-baseline policy in docs/CONFIGSCHEMA_JUSTIFICATION.md.)
# 1.4.0: MINOR — drain Never (uninhabited-type lift; macro-hygiene constraint blocked the planned migration).
# 1.3.0: MINOR — drain Color (migrated to derive in US-006).
# 1.2.0: MINOR — drain 2 generic-bound entries (VfxBindable, Pool).
# 1.1.0: MINOR — drain 8 foreign-type entries.
#
# Entries here are exempt from the justification-comment requirement introduced
# by packet 1.9.A. As of v2.0.0 the list is empty — every hand-written impl
# in the workspace carries a CONFIGSCHEMA-JUSTIFICATION comment at the source
# site. The followup packet (1.9.A.followup) is closing this loop.
#
# New impls MUST NOT be added here. New hand-written impls must carry a
# // CONFIGSCHEMA-JUSTIFICATION: <kind>: <reason>  line directly above the
# impl line, or a /// doc-comment block above containing CONFIGSCHEMA-JUSTIFICATION:.
#
# Modification of this file requires explicit reviewer approval.

schema_version = 1

# <FILE>xtask/data/configschema_baseline.toml</FILE>
# <VERS>END OF VERSION: 2.0.0</VERS>
