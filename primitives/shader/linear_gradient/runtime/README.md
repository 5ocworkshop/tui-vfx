# Runtime adapter placeholder

`shader.linearGradient` currently runs through copied compositor behavior. The first semantic runtime adapter will be added only after generated input/accessor scaffolding exists and old-vs-next parity remains green.
