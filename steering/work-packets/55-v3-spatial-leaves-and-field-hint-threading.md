# Packet 55 — V3 spatial leaves and field-hint threading

## Task first
Start the next bounded spatial-field tranche by adding the minimal spatial leaves and threading them through the recipes-side runtime where one real consumer path can prove them.

## Why this matters
The spatial-field-hint plan already names the recommended phased delivery: minimal `mixed-signals` spatial leaves first, then recipes-side threading, then typed field/hint chaining. Until that work starts, the richer flag/signal semantics remain approximated rather than properly expressed.

## Success condition
- one bounded phase from the spatial-field-hint plan lands
- the chosen spatial leaves or threading seam is test-proven
- one real V3/runtime consumer path benefits from the new data

## Mode
BLOCKER_MODE

## Task-scope paths for grounding
- `/usr/projects/tui-vfx/docs/design/tui-vfx-v3-spatial-field-hint-plan.md`
- `/usr/projects/mixed-signals/steering/INTENTIONS.md`
- `/usr/projects/tui-vfx/steering/INTENTIONS.md`
- the exact `mixed-signals` `SignalContext` / leaf files for the chosen tranche
- the exact recipes/runtime evaluation sites that must consume the new leaves

## Exact write scope
- the exact `mixed-signals` spatial-leaf files for the tranche
- the smallest recipes-side threading/evaluation files needed
- the narrowest tests proving leaf visibility and consumer use

## Out of scope
- the full field/hint architecture in one packet
- generic Madeira parity work outside the chosen consumer path
- unrelated naming cleanup

## Must-read docs in order
1. `/usr/projects/tui-vfx/docs/design/tui-vfx-v3-spatial-field-hint-plan.md`
2. `/usr/projects/mixed-signals/steering/INTENTIONS.md`
3. `/usr/projects/tui-vfx/steering/INTENTIONS.md`
4. `/usr/projects/gt-design/.omx/context/v3-managed-briefing-20260423T170500Z.md`
5. `/usr/projects/global_prompts/standards/40_ofpf_standards.md`
6. `/usr/projects/global_prompts/standards/50_tdd_protocol.md`
7. `/usr/projects/tui-vfx/steering/work-packets/COMMON_EXECUTION_RULES.md`

## Verification required
- focused mixed-signals tests for the new leaves
- focused recipes-side tests proving evaluation sites receive spatial context
- one explicit statement of which phase of the spatial-field-hint plan landed

## Task reminder
Your task is still: land one bounded spatial-leaf/threading tranche, not complete the full field-hint architecture.
