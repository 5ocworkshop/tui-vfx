<!-- <FILE>pro/EXISTING-SYSTEM-PRD/04_functional_requirements.md</FILE> - <DESC>Chapter 4 of the evidence-backed Existing-System PRD: functional requirements extracted from current code behavior. Normative language; current implementation only; no future-state requirements.</DESC> -->
<!-- <VERS>VERSION: 0.3.0</VERS> -->
<!-- <WCTX>Deepening pass: REQ-007 / REQ-012 / REQ-013 corrected; REQ-064..REQ-070 added for the V3 lowering map, the ColorSpace variant count, the production expect() panic table, and the bindable shim consolidation.</WCTX> -->
<!-- <CLOG>0.3.0: MINOR — REQ-007 (filter count) corrected from "twenty-five or more" to "thirty-three"; REQ-012 (style 50 files) corrected to "31 spatial shaders + helper files"; REQ-014 (ColorSpace) restated with the three known variants Rgb/Hsl/Hct; add REQ-064..REQ-070 covering V3 lowering coverage, expect() panic table, BindableString/U16 consolidation. 0.2.0: REQ-061..063 for tui-vfx-next. 0.1.0: initial.</CLOG> -->

# 4. Functional Requirements Extracted from Code

This chapter translates the *current* implemented behavior catalogued in chapter 3 into normative requirements. Every REQ describes what the workspace does today; no row describes desired or future behavior.

The "shall" phrasing is a normative form for the *current* contract — it asserts what the code currently guarantees, not what it ought to guarantee. The mapping `Source Feature` column links each REQ back to a chapter-3 F###; the cited evidence is the same evidence the F### subsection used.

## REQ table

| ID | Current Requirement | Source Feature | Evidence | Confidence |
|---|---|---|---|---|
| REQ-001 | The compositor shall provide free-function entry points (`render_pipeline`, `render_pipeline_with_area`, `render_pipeline_with_spec`, `render_pipeline_with_spec_area`) that take a `&dyn Grid` source, a `&RoleMap` source roles, a `&mut SemanticScene` destination, render-rect `(width, height, offset_x, offset_y)`, a spec or pre-built options, and an optional `&mut dyn CompositorInspector`. | F001 | `crates/tui-vfx-compositor/src/pipeline/orc_render_pipeline.rs:106,211`; `fnc_render_pipeline_with_spec.rs:21-30`; `fnc_render_pipeline_with_spec_area.rs:15`; `pipeline/mod.rs:35-37` | High |
| REQ-002 | `render_pipeline_with_spec` shall lower each `ShaderLayerSpec` through `ShaderWithRegion::try_from_v3_shader_family` and panic via `.expect` if the lowering fails. | F001, F009 | `crates/tui-vfx-compositor/src/pipeline/fnc_render_pipeline_with_spec.rs:25-28` | High |
| REQ-003 | When a non-`None` `inspector` is supplied to `render_pipeline*`, the per-stage observability helpers in `orc_pipeline_observability` shall emit `TraceEvent` values into the inspector. | F001, F002, F036 | `crates/tui-vfx-compositor/src/pipeline/orc_pipeline_observability.rs`; `pipeline/mod.rs:25`; `crates/tui-vfx-compositor/tests/test_inspection_sink_bridge.rs`, `test_inspection_sink_bridge_per_stage.rs` | High |
| REQ-004 | The compositor's `filters/`, `masks/`, and `samplers/` sub-modules shall be `pub(crate)`; consumers shall construct `FilterSpec`, `MaskSpec`, and `SamplerSpec` from `crates/tui-vfx-compositor/src/types/` rather than the per-`cls_*` classes. | F003, F004, F005 | `crates/tui-vfx-compositor/src/lib.rs:7-9`; `crates/tui-vfx-compositor/src/types/cls_filter_spec.rs`, `cls_mask_spec.rs`, `cls_sampler_spec.rs` | High |
| REQ-005 | The Sampler stage shall provide eleven sampler primitives (`bounce`, `crt_jitter`, `crt_sampler`, `distortion`, `fault_line`, `gravity`, `pendulum`, `radial_twist`, `ripple`, `shredder`, `sine_wave`). | F003 | `crates/tui-vfx-compositor/src/samplers/` directory listing | High |
| REQ-006 | The Mask stage shall provide eleven mask primitives (`blinds`, `cellular`, `checkers`, `diamond`, `dissolve`, `materialize`, `noise_dither`, `path_reveal`, `radial`, `spotlight`, `wipe`) and shall combine them via a `MaskCombineMode` enum. | F004 | `crates/tui-vfx-compositor/src/masks/` directory listing; `crates/tui-vfx-compositor/src/types/mask_combine_mode.rs` | High |
| REQ-007 | The Filter stage shall provide thirty-two `FilterSpec` variants (counted from the `name()` arms at `cls_filter_spec.rs:2249-2282` — 32 arms `None` through `GlyphTimeline`), with `factor`/`strength`/`progress` parameters typed `SignalOrFloat` or `BindableValue` on the rate-bearing variants. | F005, F011 | `crates/tui-vfx-compositor/src/types/cls_filter_spec.rs:443-1374, :2248-2283`; 25 `cls_*.rs` implementation files at `crates/tui-vfx-compositor/src/filters/` | High |
| REQ-008 | The shadow crate shall expose `render_shadow` and `render_shadow_into_scene` as public entry points, each accepting a `Grid` (or `SemanticScene`), an `element_rect: Rect`, a `&ShadowConfig`, and a `progress: f32`. | F006 | `crates/tui-vfx-shadow/src/lib.rs:355-359`; module rustdoc Quick Start at `:35-50` | High |
| REQ-009 | `ShadowConfig` shall expose builder methods `new(color)`, `with_offset(dx, dy)`, `with_style(ShadowStyle)`, `with_edges(ShadowEdges)`, and `with_soft_edges(bool)`. | F006 | `crates/tui-vfx-shadow/src/lib.rs:43-50` (the rustdoc Quick Start example) | High |
| REQ-010 | The shadow crate shall provide five renderer styles (`braille`, `gradient`, `half_block`, `medium_shade`, `solid`) selected via a `ShadowStyle` enum. | F007 | `crates/tui-vfx-shadow/src/renderers/` (5 `cls_*.rs` files); `crates/tui-vfx-shadow/src/lib.rs:11-12` (rustdoc enumeration); `:363` (re-export) | High |
| REQ-011 | Shadow output written via `render_shadow_into_scene` shall tag destination cells with `RoleTag::Shadow`. | F006, F027 | `Cargo.toml:7` `<CLOG>` (Phase A.3 description); `crates/tui-vfx-types/src/lib.rs:24-28` (RoleTag enumeration including `Shadow`) | High |
| REQ-012 | The style crate shall expose four public sub-modules (`models`, `schedules`, `traits`, `utils`) and shall host the V2 named-shader catalog as 31 spatial shaders (`SpatialShaderType` 31-variant enum at `cls_spatial_shader_type.rs:157`) plus helper signal/ramp/gradient/region/transition/color-space files. | F008 | `crates/tui-vfx-style/src/lib.rs:6-9`; `crates/tui-vfx-style/src/models/cls_spatial_shader_type.rs:157` | High |
| REQ-013 | The style crate's V3 surface shall live under `models/v3/` and shall provide eleven `cls_vfx_*_shader.rs` shader classes, eighteen `enum_*.rs` enum files (eleven per-shader behavior enums, five family/structure enums, two lowering-error enums), and one V2→V3 `fnc_lower_legacy_spatial_shader.rs` lowering function whose `match` covers all 31 V2 variants surjectively onto 11 V3 families (4 primitive + 7 composed). | F009 | `crates/tui-vfx-style/src/models/v3/` directory (30 files); `fnc_lower_legacy_spatial_shader.rs:23` (the `pub fn` — 31 `SpatialShaderType::*` arms verified via grep); chapter 3 F009's lowering map | High |
| REQ-014 | The `ColorSpace` enum shall expose exactly three variants — `Rgb` (default; per-channel linear), `Hsl` (shortest-path hue interpolation), `Hct` (CAM16-based via `mcu-hct`). The `Hct` variant shall route through `mcu-hct` + `mcu-utils` for perceptually-uniform brightness scaling and gradient interpolation. | F010 | `crates/tui-vfx-style/src/models/cls_color_space.rs:31`; `crates/tui-vfx-style/Cargo.toml:33-34`; `Cargo.toml:53-59` | High |
| REQ-015 | `tui-vfx-core` shall expose a `bindable` module providing `VfxBindable<T, S>`, `VfxBindableValue`, `VfxBindableU16`, `VfxBindableString`, `BindableSignal`, `RuntimeParamsRead`. | F011 | `crates/tui-vfx-core/src/lib.rs:11-15`; `crates/tui-vfx-core/src/bindable/cls_bindable.rs:24,49,73,167` (Never enum, RuntimeParamsRead trait, BindableSignal trait, VfxBindable enum) | High |
| REQ-016 | The bindable system shall provide three arms: `Literal(T)`, `Binding(name)`, and `Signal(SignalOrFloat | …)` per the documented authoring contract. | F011 | `crates/tui-vfx-content/src/lib.rs:53-90` (the rustdoc block enumerates the three arms) | High |
| REQ-017 | The `tui-vfx-content` crate shall provide fifteen text transformer classes (`Dissolve`, `GlitchShift`, `GlyphCascade`, `Marquee`, `Mirror`, `Morph`, `Numeric`, `Odometer`, `Redact`, `ScrambleGlitchShift`, `Scramble`, `SlideShift`, `SplitFlap`, `Typewriter`, `WrapIndicator`) plus a dispatcher `fnc_get_transformer`. | F012 | `crates/tui-vfx-content/src/transformers/` directory listing (15 `cls_*.rs` + `fnc_get_transformer.rs` + `fnc_morph_chars.rs`) | High |
| REQ-018 | `ContentEffect` shall expose `apply`, `apply_to_borrowed`, and `apply_with_runtime` inherent methods for the common-case animation loop. | F012 | `crates/tui-vfx-content/src/lib.rs:23-31, 33-46, 48-90` (the trio of doctest blocks) | High |
| REQ-019 | The content crate shall provide a `cursor` module and `TypewriterCursor` constructors `block()`, `underscore()`, `pipe()`, `caret()`, `simple(char)`. | F013 | `crates/tui-vfx-content/src/lib.rs:96-104` (the rustdoc doctest); `crates/tui-vfx-content/src/cursor/` directory presence | High |
| REQ-020 | The cell-motion scheduler shall expose `CellActor`, `CellMotionSpec`, `CellMotionStats`, `CellMotionVisibility`, `CellMotionAffect`, `CellMotionCoord`, `CellMotionError`, `CellMotionPhaseSpec`, `CellMotionScope`, plus enums `CellCollisionMode`, `CellPlacement`, `CellStagger`, plus public functions `apply_cell_motion`, `collect_cell_actors`, `resolve_actor_offset_ms`, `resolve_cell_placement`. | F014, F015a | `crates/tui-vfx-content/src/cell_motion/mod.rs:1-30` | High |
| REQ-021 | The mechanical content-cycle module shall be private (`mod mechanical;` — no `pub`); its surface is exposed only through the transformer family. | F015 | `crates/tui-vfx-content/src/lib.rs:116` | High |
| REQ-022 | The content crate shall provide a `glyph_particles` module with a paired test file `tests/test_glyph_particles.rs`. | F016 | `crates/tui-vfx-content/src/lib.rs:115`; `crates/tui-vfx-content/tests/test_glyph_particles.rs` | High |
| REQ-023 | The content `sources` module shall expose `RocketsplashImage`, `RocketsplashFont`, `FontRender`, and `blit_render_buffer_to_grid`, with both image and font formats sharing a `rocketsplash_rt::RenderBuffer` substrate. | F017, F018 | `crates/tui-vfx-content/src/sources/mod.rs:1-26` | High |
| REQ-024 | The content `fonts` module shall expose `FontGlyphTable`, `FontRegistry`, `DEFAULT_FONT_SENTINEL`, `line_3x3_heavy_glyphs`, `lookup_line_3x3_glyph`, `render_line_3x3_text`. | F019 | `crates/tui-vfx-content/src/fonts/mod.rs:30-33` | High |
| REQ-025 | The 3×3 line glyph table shall be the project's default font and runtime fallback per Intention 36. | F019 | `crates/tui-vfx-content/src/fonts/mod.rs:8-10`; `steering/INTENTIONS.md` Intention 36 | High |
| REQ-026 | The `assets` module shall expose `AssetRegistry` and a `DEFAULT_LOGO_SENTINEL` reserved literal that routes to the registered default at resolve time. | F020 | `crates/tui-vfx-content/src/assets/mod.rs:21` | High |
| REQ-027 | The `pool` module shall provide a generic `Pool<T>` with type aliases `EffectPool` (`Pool<ContentEffect>`), `ImagePool` (`Pool<String>`), `FontPool` (`Pool<String>`), `PresetPool` (`Pool<Preset>`), plus a `TextPool` newtype with sanitize-on-construct. | F021 | `crates/tui-vfx-content/src/pool/mod.rs:11-39` | High |
| REQ-028 | The geometry crate shall re-export `EasingType` and `ease` from `mixed_signals::easing` (geometry crate v2.0.0 is a backward-compatibility re-export shim). | F022 | `crates/tui-vfx-geometry/src/easing/mod.rs:1-7` | High |
| REQ-029 | The geometry crate shall provide nine motion-path classes (`ArcPath`, `BezierPath`, `HoverPath`, `LinearPath`, `RectilinearPath`, `SpiralPath`, `SpringPath`, `SquashPath`, `StepPath`) and shall re-export them at the `paths` module root. | F023 | `crates/tui-vfx-geometry/src/paths/mod.rs:7-26` | High |
| REQ-030 | The geometry crate shall expose `wipe_progress`, `wipe_visible_at`, and `WipeDirection` at both `geometry::wipe::*` and the crate root, providing one source of truth shared by the compositor's `Wipe` mask and the style crate's `RevealWipe` shader. | F024 | `crates/tui-vfx-geometry/src/wipe/mod.rs:23-27`; `crates/tui-vfx-geometry/src/lib.rs:23`; `crates/tui-vfx-compositor/Cargo.toml:32` (the inline-comment-justified runtime dep) | High |
| REQ-031 | The geometry crate shall expose nine public sub-modules (`anchors`, `borders`, `easing`, `layout`, `paths`, `traits`, `transitions`, `types`, `widgets`, `wipe`). | F025 | `crates/tui-vfx-geometry/src/lib.rs:7-16` | High |
| REQ-032 | The middle anchors of `calculate_anchor_position` shall return a y-coordinate at 45% of the frame height (not 50%) to preserve legacy-notification visual placement. | F025 | `crates/tui-vfx-geometry/src/anchors/mod.rs:11-13` (the `VISUAL_CENTER_PERCENT = 45` const) | High |
| REQ-033 | The foundation types crate shall expose `Cell`, `Color`, `Style`, `Modifiers`, `Point`, `Rect`, `Size`, `Anchor`, `Grid`, `BoundaryMode`, `GridExt`, and `OwnedGrid` at the crate root. | F026 | `crates/tui-vfx-types/src/lib.rs:93-109` | High |
| REQ-034 | The foundation types crate shall expose `SemanticScene`, `RoleMap`, `RoleMapIter`, `RoleTag` (with twelve first-class variants plus `Custom(InternedRoleName)`), `RoleId`, `RoleInterner`, and `SceneMetadata` at the crate root. | F027 | `crates/tui-vfx-types/src/lib.rs:14-32` (rustdoc), `:103-108` (re-exports) | High |
| REQ-035 | The foundation types crate shall expose a `glyph` module containing `cls_glyph_encoder.rs` and `fnc_sample_eight_subcells.rs` (Slice 6.6 §F.1 of the mechanical-cycles plan). | F028 | `crates/tui-vfx-types/src/lib.rs:78`; `crates/tui-vfx-types/Cargo.toml:3-4` (`<WCTX>` + `<CLOG>`); `crates/tui-vfx-types/src/glyph/` directory | High |
| REQ-036 | The foundation types crate shall expose a `braille` module providing utilities for Unicode braille patterns (U+2800–U+28FF) on a 2×4 dot grid. | F029 | `crates/tui-vfx-types/src/lib.rs:72`; `crates/tui-vfx-types/src/braille.rs:1-26` | High |
| REQ-037 | The foundation types crate shall expose `RigidShakeState` and `RigidShakeTiming` for shared timing across the RigidShake filter and style effects. | F030 | `crates/tui-vfx-types/src/lib.rs:84,102` | High |
| REQ-038 | The foundation types crate shall expose four opaque ID types — `LayerId`, `RecipeId`, `RoleId`, and `InternedString` — backed by `Arc<str>`. | F031 | `crates/tui-vfx-types/src/lib.rs:31-32` (rustdoc); `:98-103` (re-exports) | High |
| REQ-039 | The `tui-vfx-core-macros` proc-macro crate shall provide a `derive_config_schema` derive that emits `impl ConfigSchema for X` at compile time. | F032 | `crates/tui-vfx-core-macros/src/lib.rs:28`; `crates/tui-vfx-core/src/lib.rs:23` (re-export) | High |
| REQ-040 | The `tui-vfx-core` crate shall expose schema-bridge types `ConfigSchema`, `FieldMeta`, `Range`, `ScalarValue`, `SchemaField`, `SchemaNode`, `SchemaVariant`. | F032 | `crates/tui-vfx-core/src/lib.rs:16-18` | High |
| REQ-041 | The `tui-vfx-core` crate shall expose a `mixed_signals_schema` module bridging `mixed-signals` types into the ConfigSchema machinery. | F033 | `crates/tui-vfx-core/src/lib.rs:7`; `crates/tui-vfx-core/src/mixed_signals_schema.rs` (file) | High (presence) / Medium (full surface) |
| REQ-042 | The `tui-vfx-core` crate shall expose a `TimeSpec` struct with public fields `start: Instant`, `now: Instant`, `duration: Duration`, and a `progress() -> f64` method. | F034 | `crates/tui-vfx-core/src/time_spec.rs:13-22`; `crates/tui-vfx-core/src/lib.rs:9,20` | High |
| REQ-043 | The `tui-vfx-debug` crate shall expose a global `Logger` accessible via `get_global_logger`, plus per-module `LogLevel` configuration via `ModuleConfig` and `create_logger`. | F035 | `crates/tui-vfx-debug/src/lib.rs:37-38` | High |
| REQ-044 | The `tui-vfx-debug::inspection` module shall provide a fourteen-class taxonomy: `AssertingInspector`, `InspectionSink`, `PipelineSkipReason`, `PipelineStageKind`, `RoleHistogram`, `RoleMapSource`, `StageMask`, `TraceEmitter`, `TraceEnvelope`, `TraceEvent`, `TraceFilter`, `TraceReport`, `TraceSelector`, `TraceSink`. | F036 | `crates/tui-vfx-debug/src/inspection/` directory listing | High |
| REQ-045 | The inspection module shall provide a declarative filter at sink-time consisting of selector set + stage mask + frame range + time range. | F036 | `crates/tui-vfx-debug/src/inspection/mod.rs:11-19` | High |
| REQ-046 | The `tui-vfx-debug` crate shall ship two criterion benches `bench_emit_overhead` and `bench_full_trace_60fps` with `harness = false`. | F036, F047 | `crates/tui-vfx-debug/Cargo.toml:30-36` | High |
| REQ-047 | The `tui-vfx-probe` crate shall ship a binary `pipeline-probe` that accepts a `ProbeSceneSpec` JSON via `--input <path>` and emits a frame dump (default), a timeline (`--frames N`), or a frame diff (`--diff-to T`). | F037 | `crates/tui-vfx-probe/Cargo.toml:29-31`; `crates/tui-vfx-probe/src/bin/pipeline-probe.rs:7-12,42-100` | High |
| REQ-048 | `pipeline-probe` shall accept `--phase <entering|dwelling|exiting>` (default `dwelling`), `--sample-t <f64>` (default 0.5), `--cells <all|non-empty|modified>` (default `all`), `--with-causation`, and `--widget-cell` flags. | F037 | `crates/tui-vfx-probe/src/bin/pipeline-probe.rs:42-83` | High |
| REQ-049 | The `tui-vfx-probe` crate shall expose twenty-four or more `cls_probe_*` DTOs at the crate root. | F038 | `crates/tui-vfx-probe/src/lib.rs:75-99` (24 `pub use cls_probe_*::*` lines) | High |
| REQ-050 | The `tui-vfx-probe` crate shall provide a `ProbeSqliteStore` for persistence backed by `rusqlite 0.32` with the `bundled` feature. | F039 | `crates/tui-vfx-probe/src/cls_probe_sqlite_store.rs`; `crates/tui-vfx-probe/Cargo.toml:25` | High |
| REQ-051 | The `tui-vfx-probe` crate shall expose diagnostic-collection helpers `collect_basic_diagnostics`, `collect_loopback_fire_diagnostics`, `collect_probe_operational_analysis`, and a per-cell root-cause inference function `build_probe_cell_root_cause`. | F040 | `crates/tui-vfx-probe/src/lib.rs:100-104` | High |
| REQ-052 | The `tui-vfx-probe` crate shall provide a `runtime_context_from_composition` helper that produces a `ProbeRuntimeContext` from a composition. | F041 | `crates/tui-vfx-probe/src/lib.rs:91`; `crates/tui-vfx-probe/src/fnc_runtime_context_from_composition.rs` | High |
| REQ-053 | The workspace shall expose an `xtask` binary, runnable as `cargo xtask <command>` via the `.cargo/config.toml` alias, with three top-level subcommands `Audit`, `Docs`, `Recipes`. | F042 | `xtask/src/main.rs:23-39`; `.cargo/config.toml:6` | High |
| REQ-054 | `cargo xtask audit configschema` shall verify every hand-written `impl ConfigSchema for X` carries a justification comment or appears in `xtask/data/configschema_baseline.toml`. | F043 | `xtask/src/main.rs:42-52`; `xtask/src/audit/` (5 files); `xtask/tests/test_audit_configschema.rs` (15 787 bytes) | High |
| REQ-055 | `cargo xtask docs` shall provide thirteen actions: `Generate`, `Check`, `AiContext`, `Markdown`, `Validate`, `Scaffold {--write}`, `Api`, `ApiCheck`, `ApiValidate`, `ApiScaffold {--write}`, `Signals`, `SignalsCheck`, `SignalsValidate`. | F044 | `xtask/src/main.rs:54-110,147-162` | High |
| REQ-056 | `cargo xtask recipes validate` shall accept `--recipes-dir <path>` (required) and `--output-dir <path>` (default `docs/generated`) and write per-recipe reports to the output directory. | F044a | `xtask/src/main.rs:111-121,163-177` | High |
| REQ-057 | The meta-crate `tui-vfx` shall register two example targets pointing to workspace-root `examples/` files: `pipeline_effects_showcase` and `direct_api_signal_strength`. | F045 | `crates/tui-vfx/Cargo.toml:32-38`; `examples/pipeline_effects_showcase.rs`, `examples/direct_api_signal_strength.rs` (file presence) | High |
| REQ-058 | The `tui-vfx-types` crate shall declare an opt-out `serde` Cargo feature that is on by default (`default = ["serde"]`, `serde = ["dep:serde"]`). | F046 | `crates/tui-vfx-types/Cargo.toml:26-28` | High |
| REQ-059 | The `tui-vfx-geometry` crate shall ship a criterion bench named `easing` with `harness = false`. | F022, F047 | `crates/tui-vfx-geometry/Cargo.toml:35-37` | High |
| REQ-060 | The `mod mechanical;` declaration in `tui-vfx-content/src/lib.rs` shall not be `pub`, keeping the mechanical content-cycle implementation private. | F015 | `crates/tui-vfx-content/src/lib.rs:116` | High |
| REQ-061 | The `tui-vfx-next` crate shall depend only on `tui-vfx-types` and `tui-vfx-geometry`; it shall not pull in `mixed-signals`, `serde`, or any of the legacy compositor / style / content / shadow stacks. | F048 | `crates/tui-vfx-next/Cargo.toml:14-16`; module rustdoc at `crates/tui-vfx-next/src/lib.rs:3-9` | High |
| REQ-062 | The `tui-vfx-next` engine shall classify each effect by `EffectDomain::{Visual, Procedural}`; visual effects shall not write to the role channel; procedural effects may write roles. | F048, F049 | `crates/tui-vfx-next/src/effect.rs:11-26` | High |
| REQ-063 | The `tui-vfx-next` `Surface` shall pair a `tui_vfx_types::OwnedGrid` with role-tag storage and `SurfaceMetadata`; the `CellChannel` enum shall enumerate the canonical channels (`Glyph`, `Foreground`, `Background`, `Modifiers`, `ModifierAlpha`, `Role`). | F048 | `crates/tui-vfx-next/src/surface.rs:5-30` | High |
| REQ-064 | `render_pipeline_with_spec` shall lower each `ShaderLayerSpec` through `ShaderWithRegion::try_from_v3_shader_family` and panic via `.expect("spec shader layers should lower through the grouped V3 runtime seam")` on lowering failure. The lowering itself is exhaustive: 31 V2 `SpatialShaderType` variants map onto 11 V3 families (`fnc_lower_legacy_spatial_shader.rs`). | F001, F009 | `crates/tui-vfx-compositor/src/pipeline/fnc_render_pipeline_with_spec.rs:25-28`; `crates/tui-vfx-style/src/models/v3/fnc_lower_legacy_spatial_shader.rs:23` | High |
| REQ-065 | `orc_render_pipeline.rs`'s shadow path shall guard three production `.expect()` calls behind `is_some()` / `shadow_has_coverage` predicates: at `:255` on `options.shadow.as_ref()`, at `:559` and at `:582, :712` on `shadow_cell` after the `shadow_has_coverage` check at `:553`. No `unwrap()` in the production path; bounds checks via `if let Some(...)` and `is_none_or(...)`. | F001, F006 | `crates/tui-vfx-compositor/src/pipeline/orc_render_pipeline.rs:255, :553, :559, :582, :712` | High |
| REQ-066 | The compositor shall emit per-stage `TraceEvent` values into a registered `CompositorInspector` through eight distinct emit sites covering pre-stage role-map materialization, simple-stage entry/exit (shadow), per-stage entry/exit (sampler/mask/shader/filter bundle), per-cell sampler/filter/cell rendered, and per-shadow-cell application. | F002, F036 | `crates/tui-vfx-compositor/src/pipeline/orc_render_pipeline.rs:298, :299, :329, :397, :537, :556, :790, :949, :967, :991, :1018, :1058, :1076`; chapter 3 F001's emit-site table | High |
| REQ-067 | The bindable type system shall canonicalise `VfxBindable<T, S = Never>`, `VfxBindableValue`, `VfxBindableString`, `VfxBindableU16`, the `RuntimeParamsRead` trait, and the `BindableSignal` trait at `tui_vfx_core::bindable`. The style-side `BindableString` and `BindableU16` aliases at `crates/tui-vfx-style/src/models/cls_bindable_{string,u16}.rs` shall be 14-line re-export shims (`pub use tui_vfx_core::bindable::...`). The compositor-side `BindableValue` at `crates/tui-vfx-compositor/src/types/cls_bindable_value.rs:14` shall also be a re-export. | F011 | `crates/tui-vfx-core/src/bindable/cls_bindable.rs:24, :49, :73, :167`; `crates/tui-vfx-core/src/lib.rs:11-15`; `crates/tui-vfx-style/src/models/cls_bindable_string.rs:14`, `cls_bindable_u16.rs:14`; `crates/tui-vfx-compositor/src/types/cls_bindable_value.rs:14` | High |
| REQ-068 | The bindable wire format shall accept five forms: bare number (`Literal`), `{"literal": N}`, `{"binding": "name"}`, `{"signal": <SignalOrFloat>}`, plus a bare-`SignalSpec` fallback (e.g. `{"type": "sine", ...}`). | F011 | `crates/tui-vfx-core/src/bindable/cls_bindable.rs` (deserializer impl); `crates/tui-vfx-content/src/lib.rs:53-90` (rustdoc) | High |
| REQ-069 | The 4-stage compositor pipeline (Sampler → Mask → Shader → Filter) shall execute in that fixed order. Both the non-shadow `render_loop` (`:819-909`) and the shadow path's element pass (`:418-499`) follow this sequence; the shadow stage runs first when `options.shadow.is_some()` (dispatched at `:118`). | F001, F003, F004, F005, F006 | `crates/tui-vfx-compositor/src/pipeline/orc_render_pipeline.rs:118, :418-499, :819-909` | High |
| REQ-070 | The 15 content transformers shall implement the `TextTransformer` trait at `crates/tui-vfx-content/src/traits/text_transformer.rs:13` (`fn transform<'a>(&self, target: &'a str, progress: f64, ctx: &TransformContext<'_>) -> Cow<'a, str>`). Eleven rate-bearing fields across six transformers shall use `VfxBindableValue` typing (Typewriter 1, Scramble 1, GlitchShift 2, ScrambleGlitchShift 3, SplitFlap 3, Marquee 1) and call `.evaluate(progress, ctx.signal_ctx, ctx.runtime_params).unwrap_or(<sane default>)`. No production `panic!`/`unwrap()`/`expect()` in any transformer file. | F011, F012 | `crates/tui-vfx-content/src/traits/text_transformer.rs:13`; chapter 3 F012's per-transformer table | High |

## Notes

- Seventy REQ rows cover all 49 features (F001..F049 plus F015a + F044a). Some features carry multiple REQs to cover distinct contracts (F001 covers REQ-001, REQ-064, REQ-065, REQ-066, REQ-069; F011 covers REQ-015, REQ-016, REQ-067, REQ-068; F012 covers REQ-017, REQ-018, REQ-070).
- "Shall" is used as a normative form for the *current* contract surface, not as a future commitment. If the workspace's behavior changes, both the F### subsection and the REQ row must move together.
- No REQ describes desired or future behavior. Where a chapter-3 feature is `partially implemented` (F009 V3 surface, F020 asset consumer surface, F028 glyph framework Phase 3), the REQ describes only what the code currently does, with the partial-implementation status surfaced in chapter 12 (Open Questions).

<!-- <FILE>pro/EXISTING-SYSTEM-PRD/04_functional_requirements.md</FILE> -->
<!-- <VERS>END OF VERSION: 0.1.0</VERS> -->
