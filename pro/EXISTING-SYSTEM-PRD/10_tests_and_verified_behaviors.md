<!-- <FILE>pro/EXISTING-SYSTEM-PRD/10_tests_and_verified_behaviors.md</FILE> - <DESC>Chapter 10 of the evidence-backed Existing-System PRD: test-area catalog with assertion-body-derived behaviors-verified, plus coverage gaps phrased per REVERSE-PRD.md.</DESC> -->
<!-- <VERS>VERSION: 0.2.0</VERS> -->
<!-- <WCTX>Deepening pass: a sub-agent (91 tool calls) read every named integration-test file end-to-end and produced assertion-body-derived behaviors-verified. §10.2 escalated from Medium to High confidence.</WCTX> -->
<!-- <CLOG>0.2.0: MINOR — §10.2 rewritten from filename-inferred to assertion-body-derived. The pre-test categorization stays in §10.1; the per-feature-grouped behaviors-verified table replaces the prior inferred listing. 0.1.0: initial population.</CLOG> -->

# 10. Tests and Verified Behaviors

## 10.1 Aggregate test counts

The librarian index reports the following per-crate test-definition counts (`is_test = 1`):

| Crate | Test definitions | Test-peer coverage % (chapter 14) | Integration-test files (`tests/*.rs`) |
|---|---:|---:|---:|
| `tui-vfx-compositor` | 801 | 85.3 | 13 |
| `tui-vfx-style` | 670 | 59.3 | 3 |
| `tui-vfx-content` | 664 | 76.6 | 7 |
| `tui-vfx-geometry` | 215 | 77.6 | 15 |
| `tui-vfx-types` | 170 | 100.0 | 9 |
| `tui-vfx-debug` | 85 | 90.0 | 8 |
| `tui-vfx-shadow` | 84 | 100.0 | 3 |
| `tui-vfx-core` | 74 | 74.4 | 7 |
| `xtask` + `tui-vfx-next` (the `other` SQL bucket) | 46 | n/a | 1 + 1 |
| `tui-vfx-probe` | 42 | 4.8 | 6 |
| `tui-vfx` (meta) | 3 | n/a | 2 |
| **Total** | **2854** | | **75 integration-test files** |

The `test_defs` count includes both inline `#[cfg(test)] mod tests` blocks and standalone `tests/*.rs` files. The "test-peer coverage %" is the share of source files that have a paired `test_*` peer (chapter 14 records the exact `ofpf-sql` query). Coverage is highest for `tui-vfx-types` and `tui-vfx-shadow` (both 100 %); lowest for `tui-vfx-probe` (4.8 %).

## 10.2 Behaviors verified (assertion-body-derived)

Grouped by chapter-3 feature. Each test file's row records what the assertion bodies actually verify, with file:line citations and `#[test]` counts where applicable. Aggregator linker files (those that only `#[path = …] mod x;` other files via `#[path]` attributes) are noted as such.

### F001 — Render pipeline (compositor)

- **`crates/tui-vfx-compositor/tests/test_pipeline.rs`** — aggregator linker only; pulls in five `pipeline/` files via `#[path = …]` (no asserts of its own).
- **`tests/pipeline/test_helpers.rs`** — defines `render_pipeline_legacy`, `render_pipeline_with_spec_legacy`, `render_pipeline_with_spec_area_legacy` shims wrapping the role-aware entry points with `RoleMap::all_background` + `SemanticScene::from_grid_with_default_role` (no `#[test]`).
- **`tests/pipeline/test_orc_render_pipeline.rs`** — 18 tests. Verifies: direct-copy passthrough; `offset_x/offset_y` placement; `MaskSpec::Wipe` at `t=0.0`/`t=1.0`/partial; `MaskSpec::Checkers` alternation; multi-mask `Any` mode; `FilterSpec::Dim`/`Invert`/`Vignette`/`Crt` per-channel arithmetic (red dropped, fg/bg swapped, center > corner brightness, scanline alternation); `SamplerSpec::None` passthrough; sampler-chain order dependency (gravity→pendulum vs reversed yields different sampled cell); sampler-chain abort (cell skipped when later sampler returns `None`); zero-dimension no-panic; all-`t` boundaries; shadow extension into render area at `(11,2)` with `HalfBlock` style; shadow synchronised with `WipeDirection::TopToBottom`; partial-wipe top-revealed/bottom-hidden split; shadow at non-zero element offset; shadow alpha increases with `t`; grade-underlying preserves dest glyph + modifiers + `mod_alpha`; BT.601 luma drop ≥ 20 (bg) and ≥ 8 (fg); bg luma drop > fg luma drop; gradient layers create penumbra (inner_drop ≥ outer_drop); color-inert `🚀` glyph replaced with `\u{00B7}`; normal ASCII preserved.
- **`tests/pipeline/test_render_pipeline_with_spec.rs`** — 7 tests. Asserts `render_pipeline_with_spec_legacy` produces output bit-equal to `render_pipeline_legacy` (`assert_grids_equal`) for: no-effects baseline, single shader layer, every grouped V3 family (`SurfaceDepth/MotionField/EdgeDistortion/GradientReveal/ProgressEmphasis/MaterialLight/GuidanceCue/StochasticTexture/StripeMotion`), `render_pipeline_with_spec_area`, sampler chain (gravity+pendulum at `t=1.0`), shadow with `BOTTOM_RIGHT` edges, shadow `with_dramatic_grade()` over a pre-filled colored dest. Spec/options round-trip equivalence guarantee.
- **`tests/pipeline/test_multiple_effects.rs`** — 11 tests. Verifies `MaskCombineMode::default() == All`; snake-case serde round-trip for both modes; `SmallVec<[MaskSpec; 2]>` does not spill at len ≤ 2 and does at 3; identical for `SmallVec<[FilterSpec; 3]>`; `check_masks` returns true for empty list under both modes; single mask agrees across modes; two-mask `All` requires both at `(10,5)` and fails at `(10,20)`/`(60,5)`; `Any` passes if either; filter declaration order preserved.
- **`tests/pipeline/test_fnc_grade_shadow_cell.rs`** — 7 tests. `grade_shadow_cell` replaces `🚀` and PUA `\u{F121}` with `·` when `replacement_char` set; preserves ASCII `'A'` and box drawing `─`; preserves `🚀` when `replacement_char: None`; returns dest unchanged at `alpha=0` (early-return); applies bg luma reduction even when glyph is replaced.
- **`tests/pipeline/test_fnc_blend_underlying_shadow_cell.rs`** — 2 tests. Preserves dest glyph `'▁'` and fg, blends bg `(8,12,20)` with shadow bg-alpha 160 to `(3,4,7)`. Shadow-fg-coverage fallback path preserves dest glyph `'A'`/fg and produces bg `(10,15,20)`.
- **`tests/test_render_pipeline_role_awareness.rs`** — 4 tests. `RoleMap::all_background` → `dest.cell((0,0)).ch == 'X'` (direct-copy fast path). `StyleRegion::Role(Border)` paints only row 0; Text-row and Background untouched. `Role(Text)` paints only row 1. `Role(Highlight)` paints zero cells across the 6×4 grid.
- **`tests/test_render_pipeline_signature.rs`** — 4 runtime tests + 4 compile-time signature assertion functions. Compile-time `_compile_assert_render_pipeline*` functions name every parameter; renaming/dropping/reordering a parameter stops compilation. Runtime smokes call all four entry points with the role-aware shape.
- **`tests/test_alloc_budget.rs`** — 1 test using `#[global_allocator]` `CountingAllocator`. Builds a 10×10 source + default `ShadowConfig`, warms `GridPool` and `ROLES_ARC_CACHE` with one render, then asserts the `alloc_count()` delta across the second `render_pipeline` call is exactly **zero** (Phase 1a + 1b zero-alloc steady-state gate for the shadow path).
- **`tests/test_widgets.rs`** — empty since v2.0.0; V1 widget tests retired.
- **`tests/test_types.rs`** — aggregator linker for the seven `types/` files.
- **`tests/test_context.rs`** — aggregator linker.
- **`tests/context/test_cls_compositor_ctx.rs`** — 6 tests on `CompositorCtx::scratchpad_for(w,h)`. Same-size repeat returns same buffer pointer (reuse). Resize returns matching `width()`/`height()`. ARC_FUNC_01: pointer identity at same size. ARC_FUNC_02: smaller request 8×8 reuses 10×10 grid (64 > 100/2 threshold). ARC_FUNC_03: 5×5 after 10×10 falls below 50% threshold, triggers shrink; later 10×10 requires growth. ARC_FUNC_04: growth from 5×5 to 10×10 reallocates; 8×8 then reuses.

### F002 — Trace observability (debug + compositor)

- **`crates/tui-vfx-debug/tests/test_cls_stage_mask.rs`** — 6 tests. `StageMask::NONE.is_empty()`; `ALL.contains` every flag (`LIFECYCLE/RESOLUTION/COMPOSITION/PIPELINE`); bitwise OR composes; `|=` accumulates; pairwise intersections of distinct flags are empty; compile-time `assert_send_sync::<TraceSink>()` and `<TraceFilter>()`.
- **`tests/test_cls_trace_emitter.rs`** — 2 tests. Three `Arc::clone(emitter).emit()` calls from two borrowers produce `seq_in_frame == [0, 1, 2]` and all share `frame_no=7`/`t_ms=42`. `begin_frame(TraceFrameContext::new(2, 99))` resets `seq_in_frame` to 0.
- **`tests/test_cls_trace_envelope.rs`** — 3 tests. JSON serde round-trip preserves `event/frame_no/t_ms/recipe_id/seq_in_frame`; `recipe_id: None` round-trips as absent; serialised line contains no embedded `\n` (NDJSON safety).
- **`tests/test_cls_trace_event.rs`** — 18 tests. JSON round-trip for every variant (LifecyclePhaseEntered/Transition/Dismissed/Held, AssetResolved, ProceduralResolved, TokenResolved, RecipeBindingResolved, LayerStarted/CellPainted/Completed/Skipped, SamplerApplied, MaskChecked, ShaderApplied, FilterApplied, ShadowCellApplied, CellRendered). The `stage_accessor_categorizes_every_variant` test pins `LifecyclePhaseEntered → LIFECYCLE`, `AssetResolved → RESOLUTION`, `LayerStarted → COMPOSITION`, `CellRendered → PIPELINE`.
- **`tests/test_cls_trace_filter.rs`** — 6 tests. `accept_all()` admits any envelope. OR semantics across `[Cell{3,5}, Role(Border)]`. `StageMask::PIPELINE` admits `CellRendered` rejects `LayerStarted`. Frame range `5..10` admits `5,9` rejects `4,10`. Time range `100..500`. `StageMask::NONE` rejects everything; empty selectors reject everything.
- **`tests/test_cls_trace_report.rs`** — 4 tests. `TraceReport::to_ndjson` writes one parseable envelope per line with no empty lines. `from_ndjson` round-trip reproduces canonical JSON byte-for-byte; `dropped` counter not transported. `summary.count_for(stage)` correct per-stage counts. `TraceSink::with_capacity(filter, 2)` after 4 emits drops oldest 2.
- **`tests/test_cls_trace_selector.rs`** — 7 tests. `All` matches every event. `Cell{x,y}`, `Rect`, `Role`, `Layer`, `Recipe` selectors each match precisely. `LayerId::from("x") == LayerId::from(String::from("x"))` (content equality).
- **`tests/test_cls_trace_sink.rs`** — 6 tests. Insertion order preserved. Filter `[Cell{0,0}]` admits 1 of 3 emits; `dropped == 0` (filtered ≠ dropped). `with_capacity(3)` after 5 emits keeps `[2,3,4]` and `dropped == 2` (oldest-first). `StageMask::NONE` over 1000 emits → 0 envelopes, 0 drops. 4 threads × 250 emits → 1000 envelopes, no drops.
- **`crates/tui-vfx-compositor/tests/test_inspection_sink_bridge.rs`** — 4 tests. Bridge forwards every per-cell pipeline callback; minimal 4×3 source + accept-all filter produces 12 `CellRendered` envelopes plus stage events; every envelope carries `StageMask::PIPELINE`; `seq_in_frame` is monotonic. With no sampler/mask/filter/shadow, `CountingInspector` records `cells == 12, sampler == 0` (since 8dfcadf the synthetic `None#1` fallback is gone). With `LIFECYCLE`-only filter, zero envelopes emitted. With `BorderSweepShader` and `v3_family: TravelingBand`, at least one shader event label starts with `traveling_band:BorderSweep#`.
- **`tests/test_inspection_sink_bridge_per_stage.rs`** — 5 tests. Hand-built `on_stage_entered(Shader, 1, "FocusedRowGradient", "Role(Text)")` → one `StageEntered` envelope. `on_stage_finished(Filter, 2, 17, 4_200)` round-trips `cells_modified=17, elapsed_ns=4200`. `on_stage_skipped` with `PipelineSkipReason::ScopeMatchedZeroCells{predicate, role_histogram}` round-trips both fields. `on_scope_evaluated(1, 0, 320, RoleHistogram{background:320,..EMPTY})` round-trips. The full `Entered → ScopeEvaluated → Finished` sequence test asserts envelope order.
- **`tests/test_scope_mismatch_emits_zero_cell_skip.rs`** — 1 test. The 2026-04-26 `focused_row_btop` regression: 8×4 source with `RoleMap::all_background`, `Role(Text)` shader scope, `InspectionSinkBridge` installed. Asserts: exactly one `RoleMapMaterialized{source: Injected, histogram.background: 32, text: 0}`; one `ScopeEvaluated{matched: 0, skipped: 32}`; one Shader `StageSkipped{ScopeMatchedZeroCells{predicate}}` whose `predicate` contains both "Role" and "Text" and whose histogram shows `background > 0, text == 0`.
- **`tests/test_shadow_path_emits_shader_scope_skip.rs`** — 1 test. Same scenario plus `ShadowSpec`. Asserts the shadow path emits exactly one `RoleMapMaterialized` (no double-emit), Shader `ScopeEvaluated{matched: 0}`, Shader `StageSkipped{ScopeMatchedZeroCells}`, and zero `StageEntered{kind:Shader}` events (B1 strict-skip semantics — closes the architect-flagged B2 gap where the shadow path's element pass was previously observability-blind).
- **`tests/test_shadow_stage_emits.rs`** — 1 test. Default-shadow render emits exactly one `RoleMapMaterialized`, one `StageEntered{kind:Shadow}`, one `StageFinished{kind:Shadow}` whose `step_id` matches (closes the architect-flagged C1 gap).

### F003-F007 — Sampler / Mask / Filter / Shadow types (compositor types)

- **`tests/types/test_sampler_spec.rs`** — 11 tests. Default = `None`. Serde round-trip for `Axis`, `RippleCenter` (`Center` and `Point{5,5}`), and the variants `SineWave`, `Ripple`, `Shredder`, `FaultLine`, `Crt`, `CrtJitter` with full payloads. `try_from_v3_payload` for `sine_wave` lifts `phase_offset` into `phase=Static(0.25)`; `ripple` with `center: {kind: "center"}` builds `RippleCenter::Center`.
- **`tests/types/test_mask_spec.rs`** — 11 tests. Default = `None`. Serde round-trip across `WipeDirection` (4 variants), `Orientation` (2), `IrisShape` (3), `DitherMatrix` (2), and `MaskSpec::{Wipe, Dissolve, Checkers, Blinds, Iris, Diamond, NoiseDither, Materialize}` with full payloads.
- **`tests/types/test_filter_spec.rs`** — 13 tests. Default = `None`. Serde round-trip for `Dim`, `Invert`, `Tint`, `Vignette`, `Crt`, `Greyscale`, `EdgeGrow`, `AnimatedGlyphRamp`. `try_from_v3_payload` for `rigid_shake` lifts `num_shakes: {binding: "error_severity", default: 4}` into `num_shakes=4, num_shakes_binding=Some("error_severity")` and `damping_scale_binding=Some("severity_decay")`. `AnimatedGlyphRamp` with no colors and no gradient fails `validate()`. `colors:[Red]` (single entry, mismatched cardinality) also fails. V3 payload with `color_gradient.space:"rgb"` validates and yields `ColorSpace::Rgb`. `ApplyTo` parses `"fg"` and PascalCase `"Fg"`; serializes as `"foreground"`.
- **`tests/types/test_composition_options.rs`** — 7 tests. `with_shader_layer` without v3 hint → `v3_shader_families().is_empty()`. Manual push of two `ShaderWithRegion` with `v3_family: Some(TravelingBand)` and `Some(SurfaceDepth)` exposes both. `try_with_v3_shader_family` builds `shader_layers[0].shader_label == Some("Glow")`. `with_playback_timing(1.3, Some(1.4), Some(Phase::End))` clamps to `t=1.0, loop_t=Some(1.0), phase=Some(End)`. `from_options` falls back to phase progress when `loop_t` absent. `effective_samplers()` falls back to legacy `sampler_spec`; `with_samplers([gravity, pendulum])` preserves order.
- **`tests/types/test_composition_spec.rs`** — 7 tests. Empty default → zero v3 families. Mixed `[BorderSweep, Glow]` → `[TravelingBand, SurfaceDepth]`. `try_push_v3_shader_family` and `try_with_v3_shader_family` both work. `apply_playback_timing` clamps. `from_spec` exposes `effective_loop_t() == 0.75`.
- **`tests/types/test_shader_layer_spec.rs`** — 4 tests. `BorderSweep → ComposedPrimitive(TravelingBand(_))` and `Glow → Primitive(SurfaceDepth(_))` via `v3_shader_family()`. `try_from_v3_shader_family` round-trips a `Glow` family back to `SpatialShaderType::Glow(_)`.
- **`tests/types/test_bindable_value.rs`** — 13 tests (verifies REQ-068 wire format). Post-1.2.A consolidation: `BindableValue` is `VfxBindable<f32, SignalOrFloat>`. `Signal(Static(0.5)).evaluate()` → `Some(0.5)`. `Binding("progress")` returns `Some(0.7)` when `runtime_params.insert("progress", 0.7)` and `None` when missing. Binding lookup ignores `loop_t`. `From<f32>` and `From<SignalOrFloat::Static(v))` both collapse into `Literal(v)`. `Default()` is `Literal(0.0)`. JSON wire shapes: bare `0.75` → `Literal(0.75)`; `{"signal":0.5}` → `Signal(Static(0.5))`; `{"binding":"progress"}` → `Binding`; `Literal(0.5)` serialises as `{"literal":0.5}`.

### F008 / F009 — V2/V3 spatial shaders (style models, role awareness)

- **`crates/tui-vfx-style/tests/test_models.rs`** — aggregator linker registering 26 sub-test files (one per `cls_*` shader / model) covering: `AffordanceWakeShader, ColorConfig, ColorSpace, ConcealedLightShader, CursorShader, DiffusionShader, EasingType, FadeToBlack, FocusFieldShader, FocusedRowGradientShader, GlistenBandShader, Gradient, GradientLut, LinearGradientShader, NeonFlickerShader, SpatialShaderType, StyleEffect, StyleLayer, StyleRegion, StyleTransition, WayfindingNodeShader`, `effect_descriptions`, `fnc_apply_style_effects_to_scene`, `fnc_style_region_bounding_rect`, `fnc_style_region_should_style`, `serde_roundtrip`, `style_region_legacy_parse_round_trip`, `terminal_fire_recipes`. Behaviors: per-shader serde round-trip, `style_at(ShaderContext, base_style)` arithmetic per shader, `StyleRegion::should_style((x,y), role)` predicate evaluation, legacy V1 JSON shapes parse into V2/V3 in-memory variants.
- **`tests/test_shader_role_awareness.rs`** — 4 tests. Default `ShaderContext.roles` is empty (zero-dim → every `get` returns `None`). With `RoleMap::new_with_default(8,4, Background)` and `(3,1) → Border`, a hand-built `RoleAwareShader` reads `ctx.roles.get((ctx.local_x, ctx.local_y))` and flips fg to `RED` only at the Border cell; non-matching cell preserves `WHITE`. `assert_send_sync::<ShaderContext>()`. `ShaderContext::clone()` preserves the role map's `Title` tag at `(2,2)`.
- **`tests/test_utils.rs`** — aggregator linker for 4 utility sub-files (`fnc_blend_colors`, `fnc_brighten_hct`, `fnc_color_ops`, `fnc_easing`).

### F010 — ColorSpace

Coverage lives in the linker-registered `tests/models/test_cls_color_space.rs`. `ColorSpace::Rgb` round-trips through `AnimatedGlyphRamp` filter coverage in `test_filter_spec.rs:209`.

### F011 — Bindable

Cross-crate: `crates/tui-vfx-compositor/tests/types/test_bindable_value.rs` (13 tests above), plus `RigidShake` binding payload in `test_filter_spec.rs:134-160`, plus `selected_row_binding` runtime resolution proven through probe at `crates/tui-vfx-probe/tests/test_probe_frame_dump.rs:277` (`runtime.binding_resolutions[].field == "selected_row"`).

### F012 — Content transformers

- **`crates/tui-vfx-content/tests/test_transformers.rs`** — aggregator linker for 11 sub-files (`test_cls_marquee, test_cls_numeric, test_cls_odometer, test_cls_odometer_cycles, test_cls_redact, test_cls_scramble, test_cls_slide_shift, test_cls_split_flap_tiles, test_cls_typewriter, test_fnc_get_transformer, test_typewriter_transform_with_cursor`). Each per-class file exercises `transform(target, progress, &TransformContext)` returning `Cow<str>` with progress-driven reveal/redaction/shuffle output.
- **`tests/test_content_effect_apply.rs`** — 9 tests. `Typewriter.apply("Hello", 0.0)` → `""`; at 1.0 → `"Hello"`; at 0.5 → non-empty proper prefix. `apply_to_borrowed("Hello", 1.0)` returns `Cow::Borrowed` (zero-alloc fast path at full reveal). `apply()` matches `get_transformer().transform(target, progress, &TransformContext::new(&sig, &params))` for both Typewriter and Scramble. `apply_with_runtime` with non-default `SignalContext{frame:42, seed:99, width:80, height:24}` produces output equal to the explicit dispatcher path. Empty-target and single-character-target ("X") edge cases.
- **`tests/test_typewriter_cursor.rs`** — 9 tests. `TypewriterCursor::simple('X')` sets `cursor.character = "X"` and preserves default `blink/show_while_typing/show_after_complete`. `block()/underscore()/pipe()/caret()` produce `"█"/"_"/"|"/"▌"` and each is exactly one grapheme cluster. `block() == default()` and `simple('█') == default()`. Backcompat: v1.1.0 JSON `{"character":"█","blink_interval":500,"show_while_typing":1.0,"show_after_complete":1.0}` lifts `blink_interval` into `cursor.blink.interval_ms` and defaults the new `cursor.grow_in/wake` fields.

### F013 — Cursor

- **`tests/test_cursor.rs`** — aggregator linker for 15 cursor sub-files (`test_cls_cursor[_blink|_grow_in|_wake|_scan|_state|_paint_ops]`, `test_fnc_[advance_cursor|render_cursor|typewriter_cursor_position|splice_cursor_into_text|apply_ghost_glyphs_to_grid|build_cursor_shader|cursor_grow_in_glyph|cursor_scan_glyph]`). Each is a class- or function-scoped TDD red→green test asserting state transitions, painted-cell coordinates, glyph splicing into a string, ghost-glyph blending into a grid, and `CursorShader` construction.
- **`crates/tui-vfx-compositor/tests/cursor_integration.rs`** — 2 tests. `Typewriter::transform_with_cursor("hello", 0.6, &ctx, &cursor, &mut state, 0.0, 0.016)` produces text containing `'█'` and `starts_with("hel")` (5 graphemes × 0.6 = 3 revealed). Two-frame advance produces non-empty `ops.trail`. `fnc_build_cursor_shader(&ops, &cursor.wake)` builds a `CursorShader` installed via `SpatialShaderType::Cursor`; `render_pipeline_with_spec_legacy` paints the trail position with fg shifted away from source grey `(200,200,200)` (proves shader dispatched and Tint blend touched the cell).

### F014 / F015a — Cell motion / glyph particles

- **`tests/test_cell_motion.rs`** — 20 tests on `apply_cell_motion(scene, spec, timing, frame, options)`. Middle-out from `Anchor::Center` collapses 3 cells to position 1 with `collision_count == 2`. Unselected cells outside `CellMotionScope::Cells` remain at authored positions. Selected cells vacate authored origin (`(0,0)` becomes `' '` after move). `CellCollisionMode::PreserveExisting` keeps the dest occupant; `NearestToCompletion` tie-breaks at `t=0.5` to the latest mover. Zero-duration enter teleports `from→to`; zero-duration exit hides. Stagger longer than phase produces `hidden_before_start_count == 0`. `CellStagger::Random{seed:7, max_offset_ms:50}` produces equal `max_stagger_offset_ms` across two runs (deterministic). `CellPlacement::Absolute{x:-1, y:0}` clips with `clipped_actor_count == 1`. `RoleTag::Title` survives the move. Empty-cell selection produces `selected_actor_count == 0`. CJK `"界界"` selects 2 cells (cell-based, not grapheme-based currently). `reduced_motion: true` enter teleports to `to` after stagger; exit hides after stagger. `quantize_steps: Some(0)` and `Some(1)` both fail `validate()` with `InvalidQuantizeSteps`. `stride_ms: u64::MAX` does not underflow. `baseline_overwrite_count == 1` when an unselected cell is overwritten. Non-zero local frame `Rect::new(10,5,2,1)` returns scene-local coordinates with `clipped_actor_count == 0`.
- **`tests/test_glyph_particles.rs`** — 5 tests on `emit_glyph_particles(scene, spec, timing, frame, options)`. `AuthoredOffset{1,0}` spawns a `'0'` glyph with `Color::CYAN` over the source `'A'` at `(1,0)`; `source_actor_count==1, emitted_particle_count==1`. After `lifetime_ms=10` at `t_ms=20`, particle despawns and `completed_particle_count==1`. `ParticleEndBehavior::FreezeInPlace` keeps the `'0'`. `ParticleConcurrency::RoundRobin{stride:2}` over 4 source actors yields `candidate=4, emitted=2, skipped_by_concurrency=2`. `RandomSample{fraction:0.5}` with `seed:7` produces equal stats and grid cells across two runs (deterministic).

### F022 / F023 — Easing / Paths (geometry)

- **`crates/tui-vfx-geometry/tests/test_easing.rs`** — aggregator linker (`test_fnc_ease`).
- **`tests/test_paths.rs`** — aggregator linker for 7 path classes (`arc, bezier, hover, linear, spiral, spring, squash`). Each per-class file asserts trajectory points at `progress=0/0.5/1.0` against expected coordinates.
- **`tests/test_types.rs`** — linker for 7 type sub-files (`bezier_curve, cls_easing_curve, motion_spec, placement_spec, rect_scale, timeline, transition_spec`). Each tests deterministic interpolation/sampling.
- **`tests/test_layout.rs`** — linker for `test_fnc_grid_snap`.

### F024 — Wipe / slide path / vanishing edge

- **`tests/test_legacy_parity.rs`** — 4 tests. `calculate_anchor_position` for all 9 `Anchor` variants returns hand-computed positions including the **45 % middle-row visual offset** (`50 * 45 / 100 == 22`) verifying REQ-032. `resolve_slide_direction(Default, anchor)` for all 9 anchors maps to expected `From*` direction. `slide_offscreen_position(FromLeft, ...)` returns `(-21, 25)` (1-cell margin); 4 directions covered. `slide_calculate_rect_path` at `t=1.0`/SlidingIn returns full rect; at `t=0.0` returns `Rect::default()`; at `t=0.6` produces width=2 (`lerp(16,11,0.6)=13`, visible `[13,15)`). `expand_collapse_calculate_rect` at `t=0` returns 3×3 centered on full-rect center; at `t=1` returns the full rect.
- **`tests/test_slide_path.rs`** — 3 tests. `SlidePath::toast(dwell)` produces a static path: at any `t` and either phase, the rect equals `dwell`. Custom `start→dwell→end` slide: `SlidingIn` from `start(0,3,6,3)` to `dwell(17,3,6,3)` interpolates `(9,3,6,3)` at `t=0.5`; `SlidingOut` produces `(26,3,6,3)`.
- **`tests/test_slide_arc_interpolation.rs`** — 2 tests. `PathType::Arc{bulge:0.50}` at `t=0.5` for a left-to-right segment bends down (positive bulge).
- **`tests/test_slide_bounce_interpolation.rs`** — 1 test. `PathType::Bounce` hits target then bounces back.
- **`tests/test_slide_carrier_orbit_figure_eight_interpolation.rs`** — 4 tests. `PathType::CarrierOrbit{rotations:1.0, radius:4.0}` returns `(0,0)` at `t=0`, `(10,0)` at `t=1`, deviates from carrier at `t=0.25`. Helix alias deserializes to `CarrierOrbit`. `FigureEight` crosses center. Infinity alias deserializes to `FigureEight`.
- **`tests/test_slide_composed_interpolation.rs`** — 2 tests. `PathType::Composed{route: Arc, dynamics:[Orbit]}` at `t=0.25` differs from plain Arc.
- **`tests/test_slide_direction_defaults.rs`** — 1 test. `resolve_slide_direction(Default, anchor)` table-driven mapping.
- **`tests/test_slide_swirl_attractor_interpolation.rs`** — 3 tests. `PathType::Swirl{rotations:0.5, radius:4.0}` preserves endpoints `(0,0)→(10,0)`. `PathType::Attractor` preserves endpoints. Both verify mid-route deviation.
- **`tests/test_position_spec.rs`** — 1 test. `PositionSpec::FramePermille{x_permille:150, y_permille:500}.resolve_in_frame(Rect::new(10,20,100,50))` → `Position::new(25, 45)`.
- **`tests/test_fnc_vanishing_edge_trim_spec.rs`** — 2 tests. For `SlideDirection::FromRight` with right-clipped visible rect, the spec returns `right=Blank, top_right=Horizontal, bottom_right=Horizontal, left/top/bottom=Keep`. Returns `None` when not clipped.
- **`tests/test_widgets.rs`** — linker for 3 widget files (`col_numpad_mapping`, `fnc_hit_test_numpad_grid`, `fnc_resolve_direction_selection_motion`).

### F025-F031 — Anchors / Grid / SemanticScene / RoleMap / RoleTag / Opaque IDs (types)

- **`crates/tui-vfx-types/tests/test_role_tag.rs`** — 5 tests. All 12 first-class variants round-trip through `from_shorthand("background"…)` / `shorthand_name()`. `from_shorthand("custom:foo") == from_shorthand("foo") == Custom(foo)`. Wildcard match arm compiles. Serde round-trip (gated `cfg(feature="serde")`).
- **`tests/test_role_id.rs`** — 4 tests. `RoleId` is `Copy`. Equality and ordering. `id()` accessor returns inner `u16` (covers `0` and `u16::MAX`).
- **`tests/test_role_interner.rs`** — 5 tests. First-class IDs stable `0..=11` regardless of intern order. Custom IDs start at 12; re-intern of same name returns same ID. `resolve(RoleId(4))` returns `Some(RoleTag::Border)`. Custom resolve preserves original interned name.
- **`tests/test_role_map.rs`** — 13 tests. `empty(4,3) → all Background`. `empty == all_background`. `new_with_default(3,2, Border)` paints every cell. Set/get round-trip in bounds. Out-of-bounds get returns `None`; out-of-bounds set is a no-op (no panic, no mutation, no generation bump). Iter is row-major. `generation()` starts at 0; in-bounds set bumps; multiple sets bump monotonically; clone preserves generation. Serde round-trip preserves cells.
- **`tests/test_semantic_scene.rs`** — 7 tests. `SemanticScene::new(grid, roles)` with matching dims succeeds; `area() == Rect(0,0,4,3)`. `#[should_panic(expected="dimension")]` on grid 4×3 + role-map 5×3. `cell((1,1)).ch == 'X'` parity with `grid().get(1,1)`. Role accessor parity with the `RoleMap`. `with_metadata(SceneMetadata{...})` preserves grid and roles. `from_grid_with_default_role(grid, Image)` tags every cell `Image`. `roles_mut()` allows in-place set.
- **`tests/test_layer_id.rs`** — 5+1 tests. `LayerId::from(&str)` and `From<String>` produce equal opaque IDs. `cfg(feature="serde")` JSON round-trip.
- **`tests/test_recipe_id.rs`** — Mirror of LayerId tests.
- **`tests/test_interned_string.rs`** — 7 tests. Equality by content (allocation independence). Distinct strings differ. `as_str()` round-trip. `empty()` sentinel returns `""`. `Send + Sync`. Hashes by content (`HashSet` of two equal `"a"` collapses to size 1).
- **`tests/test_scene_metadata.rs`** — 3 tests. `Default` has all fields `None` / `layer_count=0`. `#[non_exhaustive]` forces struct-update construction. Serde round-trip for full and default metadata.

### F032 / F033 / F034 — ConfigSchema derive / mixed-signals schema / TimeSpec (core)

- **`crates/tui-vfx-core/tests/test_cls_schema_registry.rs`** — 9 tests. `SchemaRegistry::new`/`default` empty. `register/get/iter/type_names`. Re-registering overwrites. New struct fields (`description`, `json_name`) and enum fields (`tag_field`) round-trip.
- **`tests/test_derive_macro_phase2.rs`** — 5 tests. `#[derive(ConfigSchema)]` with `#[serde(rename = "test_config")]` produces `description = Some("A test struct with documentation")` (lifted from doc comment) and `json_name = Some("test_config")`. Per-field doc comments lift into `meta.description`. `#[serde(rename = "customField")]` populates `json_key`. `Option<i32>` field has `meta.optional == true`. Enum-level doc lifts into description; `#[serde(tag = "type")]` sets `tag_field = Some("type")`. `#[serde(rename_all = "snake_case")]` produces `json_value = Some("first_variant")`.
- **`tests/test_fnc_to_json_schema.rs`** — 9 tests. Produces `$schema = "http://json-schema.org/draft-07/schema#"`, `title`, `definitions.Person.type = "object"`, properties carry `type/description`, `required`, optional fields are `anyOf` with null. Unit enums emit `type:"string"` + `enum` array. Tagged enums emit `oneOf` with each variant carrying `properties.type.const`. Nested struct → `$ref`. `Vec<String>` → `type:"array"`. Default values appear as `default`. Range constraints become `minimum/maximum`.
- **`tests/test_fnc_to_markdown.rs`** — 8 tests. Markdown output contains H1 title, H2 type-name section, description prose, `### Fields` table, defaults rendered as backticked literals. Enum unit-variants render `### Values` table. Tagged enum renders `**Discriminator field:** \`type\`` and `#### \`circle\``. Nested types produce `[\`Point\`](#point)` cross-reference links.
- **`tests/test_manual_schema.rs`** — 1 test. Verifies `SchemaNode::Enum` and `FieldMeta` can be hand-constructed.
- **`tests/test_schema_output.rs`** — 1 test. Pins exact JSON output of `ExampleEnum::schema().to_json_pretty()` byte-for-byte against a 137-line expected string covering Unit/Tuple/Struct variants, range constraints, default values, `#[config(help)]`, `#[config(opaque)]`, `#[config(hidden)]`.
- **`tests/test_time_spec.rs`** — 4 tests. `progress()` returns 0.5 at midpoint, clamps to 1.0 when `now > start+duration`, returns 1.0 for zero duration, saturates to 0.0 when `now < start`.

### F037-F041 — Pipeline-probe binary, DTOs, SQLite, diagnostics

- **`crates/tui-vfx-probe/tests/test_probe_diagnostics.rs`** — 5 tests. `collect_basic_diagnostics(&report)` over `["A──╮","│OK│","╰──Z"]` reports both `alpha_on_top_border` and `alpha_on_bottom_border`. `╰▁▁╯` triggers `underline_on_bottom_border`. Clean borders produce zero diagnostics. Plain text rows produce zero diagnostics (no false positives). `row_text` preserves horizontal gaps.
- **`tests/test_probe_frame_dump.rs`** — 7 tests. `run_probe(&scene, &request)` with `ProbeCellSelector::All` returns `cells.len() == 6` matching the 3×2 source, `widget.abs_origin == (2,1)`, `widget.size == (3,2)`, `summary.modified_cells == 0`. With `FilterSpec::Dim` and `Modified` selector, every cell carries `last_touch.stage == "filter"`. With `BorderSweep` shader, `pipeline.shader_families` contains `"traveling_band"` and every cell's `last_touch.stage == "shader"`. `with_causation: true` + Dim filter: every cell's trace has length 1 and `trace[0].stage == "filter"`. Border-corrupted source auto-populates diagnostics directly in the report. `cells.len() != width*height` returns `Err` containing `"invalid scene"`. `FocusedRowGradient` with `selected_row_binding: Some("selected_row")` and `runtime_params {"selected_row": 3}` produces `runtime.supplied_params[0].key == "selected_row"`, `binding_resolutions[].field == "selected_row"`, and at least one cell with `root_cause.summary.contains("shader")`.
- **`tests/test_probe_operational_analysis.rs`** — 4 tests. `collect_probe_operational_analysis("frame", &[report])` for a Dim filter reports `combined.status == Success` (other stages with `configured_count == 0` classify as `Inactive`); `effects[0].effect == "Dim#1", configured_instances==1`. Shader stage exposes `effect == "BorderSweep#1", family == Some("traveling_band")`. Multi-sampler chain produces `configured_count == 2`, `observed_effects` contains `"SineWave#1 -> Ripple#2"`, both samplers status `Success`. Border-corrupt scene produces `combined.status == Failure` with `diagnostic_codes` containing `"alpha_on_top_border"`.
- **`tests/test_probe_sqlite_store.rs`** — 6 tests. `ProbeSqliteStore::new_in_memory()` ingests reports and exposes SQL queries: `probe_cells` count==6 for a Dim 3×2; `probe_trace_events where stage='filter'` count==6; before/after bg snapshots present. `ingest_timeline` over 3 frames yields `probe_frames` count==3. `ingest_diff` populates `probe_diff_cells` with `changed_cells_count > 0`. `ingest_operational_analysis_value` and `ingest_lifecycle_analysis_value` populate `probe_analysis_stages` (3 rows for `stage='filter'`). Border-corrupt scene populates `probe_diagnostics` with 3 rows including `severity='error'`. Binding scene populates `probe_runtime_params` (`key='selected_row', kind='integer'`), `probe_binding_resolutions` (`field='selected_row'`), and `probe_cell_root_causes` with `dominant_stage='shader'`. `ingest_motion_analysis_value` populates `probe_motion_effects` with `effect='KittScanner#1', span_x=37.0, status='success'`.
- **`tests/test_probe_timeline_diff.rs`** — 3 tests. `run_probe` with causation + Dim filter records `trace[0].before.fg.r == 255` and `trace[0].after.fg.r == 128`. `collect_timeline(scene, Dwelling, 3, request)` produces `frame_count == 3`, `frames[0].timing.requested_t == 0.0`, `frames[2].timing.requested_t == 1.0`. `run_probe_diff(scene, Dwelling, 0.0, 0.5, true)` returns `kind == "frame_diff"`, `changed_cells_count > 0`, every changed cell has `trace[0].stage == "shader"` and either fg or bg differs.
- **`tests/test_probe_widget_cell_focus.rs`** — 1 test. `find_widget_cell(&report, 0, 0)` returns a cell with `widget_local == (0,0)` and `root_cause.is_some()`.

### F006-F007 — Shadow (shadow crate)

- **`crates/tui-vfx-shadow/tests/test_cls_shadow_config.rs`** — 7 tests. `default().source_region == None`. `with_source_region(Border)` sets `Some(Border)`. Repeated calls overwrite (last-wins). Serde JSON round-trip preserves `source_region == None`, all 12 first-class variants, and `Custom("card-surface")` inner name.
- **`tests/test_fnc_extract_shadow_envelope.rs`** — 7 tests. `source_region: None` masks every non-empty source cell (3×2 rect at `(1,1)` → mask matches exactly). `Some(Border)` over a Border-tagged top row + Text-tagged interior masks only `y == 0`. Border filter on all-Text grid yields zero cells (`bounding_rect == None`). 1×1 source with Border tag: mask is set, `bounding_rect == Rect::new(0,0,1,1)`. 2×1 source with Border at `(0,0)` only: mask `(0,0)=true, (1,0)=false`. Zero-dim source returns empty 0×0 mask. Tight bounding rect shrinks to role-tagged sub-rect. Dimension mismatch (4×4 roles vs 6×4 grid) uses the smaller bounds.
- **`tests/test_fnc_render_shadow.rs`** — 5 tests. `render_shadow_into_scene` with `source_region: None` produces grid bit-equal to legacy `render_shadow(grid, rect, config)` (cell-by-cell over 20×10). Shadow stage writes `RoleTag::Shadow` into the destination role map at every cell it produced. `with_source_region(Border)` over a 2-row border (`y=2..4`) at offset (2,1) leaks no shadow cells into rows `y >= 5`. All-Text role map with `source_region: Border` produces zero non-empty cells.

### F042-F044 — xtask audit configschema

- **`xtask/tests/test_audit_configschema.rs`** — 9 tests using `tempfile::TempDir` fixtures + `make_fixture(&[(rel_path, content)])` helper. Unjustified `impl ConfigSchema for X` in a new file in warn-only mode succeeds. Unrecognized kind (`"derive-cannot-handle-magic"`) is always a hard error regardless of mode. Canonical justification (`derive-cannot-handle-untagged-enum: …`) passes. Baseline TOML entry exempts an unjustified impl. Stale baseline pointing to a deleted file does not panic. Macro-body matches (containing `$t:ty`) are skipped. `Other("…")` justification passes with a warning. **Qualified-path** `impl tui_vfx_core::ConfigSchema for X` with unrecognized kind is a hard error (proves the scanner finds qualified-path impls — packet 1.9.A.followup US-002 fix). Qualified-path with canonical justification passes end-to-end.

### F048-F049 — tui-vfx-next surface contract

- **`crates/tui-vfx-next/tests/test_surface_contract.rs`** — 22 tests on `SurfaceEngine` and `SurfacePipeline`. `copy(source, dest, ScopeSpec::All)` preserves sampled source roles per cell. `apply_dim` with `DimEffect::new(0.5)` keeps the destination's pre-existing role (Border). `ScopeSpec::Role(Text)` writes only Text-tagged cells; non-matching cells preserve dest cell `'y'/'z'` and dest role `Highlight`. `ScopeSpec::Rect(Rect::new(0,0,1,1))` writes only `(0,0)`. `ScopeSpec::Role(Shadow)` over a non-Shadow source emits exactly one `SurfaceDiagnosticCode::ZeroCellScope` diagnostic. `ExplicitRoleWriteEffect::new(cell('░'), Shadow)` with `ColumnRange{1,2}` writes only column 1. `Cell::default()` is written under `WriteCell` policy but skipped under `SkipTransparentEmpty`. `RoleSpace::SampledSource` (default) and `RoleSpace::Destination` produce different write counts when scope predicate consults different role sources. `ShiftSampler::new(1, 0)` shifts source: `[A,B,C]` becomes `[B,C,(unchanged)]`. Out-of-bounds sample preserves dest. Pipeline tests: stage 2 reads stage 1 output (proves Stage 2 cannot pass by reading the original input). Stage order is semantic. Visual stage preserves prior stage roles (Title survives Dim). Diagnostic message contains the stage name; `path == "pipeline.stage[0].missing-shadow-stage"`. Phase B sampled-role semantics: `ScopeSpec::Role(Text)` reads from current surface (stage 1's role write), not original source.

### tui-vfx (top-level smoke)

- **`crates/tui-vfx/tests/test_foundation_end_to_end.rs`** — 1 test composing all four Sub-plan A phases. Builds 10×6 source with 4 distinct role-tagged rows (Border row 0+5, Text rows 1/3/4, Image row 2, Background default). Border-targeted `LinearGradientShader`, `ShadowConfig::with_source_region(Border)`, `InspectionSinkBridge` over `StageMask::PIPELINE` filter. Asserts: dest scene has at least one `RoleTag::Shadow` cell; no Shadow role inside the element rect; report has at least one envelope, every envelope's stage contains `PIPELINE`, every envelope's `recipe_id == "foundation.e2e.v1"` and `frame_no == 0`; at least one `CellRendered` and one `ShadowCellApplied` event; `seq_in_frame` strictly monotonic; NDJSON `to_ndjson` → `from_ndjson` round-trip preserves envelope count, `frame_no, seq_in_frame, t_ms, recipe_id, event` for every envelope.
- **`tests/test_full_schema_dump.rs`** — 2 tests. Dump-only diagnostic tests: `print` `MaskSpec::schema().to_json_pretty()`, `FilterSpec`, `SamplerSpec`, `StyleEffect`, `FadeSpec`, and the JSON Schema draft-07 form via `to_json_schema(&MaskSpec::schema(), "MaskSpec")`. **No assertions** — these run for visual inspection only. Confidence: low; these are not regression gates.

## 10.3 Coverage gaps

Phrased per `pro/REVERSE-PRD.md` §"Tests": "No test covering X was found during inspection of these files," not "X is untested."

- **F019 — 3×3 line glyph default font.** No dedicated integration test in scope. Coverage flows through inline `#[cfg(test)]` modules within each `cls_*` font file.
- **F020 — Asset registry consumer surface.** No dedicated integration test in scope. Coverage is indirect through the `TraceEvent::AssetResolved` round-trip in `test_cls_trace_event.rs:69`.
- **F021 — Pool primitives.** No dedicated integration test. Indirect coverage via `test_alloc_budget.rs` (the zero-alloc gate).
- **F045 — Examples.** No dedicated integration tests; coverage is via running the example binaries.
- **F046 — Serde feature.** Gated tests in the types crate (`LayerId`, `RecipeId`, `SceneMetadata`, `RoleMap`, `RoleTag`) under `cfg(feature="serde")`; not flagged as a gap, but recorded as a partition.
- **F047 — Benches.** Not under `tests/`.
- **F015 — Mechanical content cycles.** No dedicated `test_mechanical*.rs` (the surface is private — REQ-021/REQ-060). Coverage via the transformer-family tests.
- **`compositor/widgets/`** is empty post-V1-removal (chapter 12 §12.2). No widget tests exercise this directory.
- **No `test_rocketsplash_image*.rs` or `test_rocketsplash_font*.rs`** in `crates/tui-vfx-content/tests/`. The sources have inline `#[cfg(test)]` modules; no dedicated integration test loads a real `.rss` or `.rsf` byte sequence and verifies the blit.
- **`tui-vfx-probe`** has 4.8 % test-peer coverage (chapter 14). Per-`cls_probe_*` DTO coverage relies on integration paths (the 6 `tests/*.rs` files above) rather than per-file unit tests. **No dedicated `test_cls_probe_*` peer files were found** during inspection of `tui-vfx-probe/src/`.
- **The empty `[features]` blocks** in `tui-vfx-geometry` and `tui-vfx-content` (chapter 2 §2.5) gate no behavior; no test exercises a feature-gated path because there are no gated paths.

## 10.4 Tests are evidence, not proof

The catalogued tests are evidence of intended behavior. They are not exhaustive proof. When chapter 3's per-feature subsections claim "High" confidence, the citation includes a test file or `#[cfg(test)]` module that demonstrates the contract. When the subsection claims "Medium" confidence, the test surface is partial or the per-variant parameter shape relies on inline `#[cfg(test)]` blocks not enumerated at this audit's resolution.

## 10.5 Confidence

After the deepening pass, **High** confidence on the assertion-body-derived behaviors-verified for: compositor F001/F002, debug/inspection F002, types primitives F031/F027, shadow F006/F007, probe F037-F041, tui-vfx-next surface contract F048/F049, core schema F032, xtask audit F042-F044, and the V3 family lowering surface F008/F009 (for the spec/options round-trip equivalence; per-shader output remains aggregator-linker-only).

**Medium** confidence on: per-shader spatial output (covered by linker-only files in `crates/tui-vfx-style/tests/models/` — verified to exist as 26 distinct files but not individually read in this pass; the aggregator linker `test_models.rs` registers each), per-cursor-class behavior (15 cursor sub-files registered by `test_cursor.rs`, not individually read), per-transformer-class output (11 sub-files registered by `test_transformers.rs`, not individually read), per-path interpolation correctness (7 path-class files registered by `test_paths.rs`, sampled but not all read), per-easing-function correctness (1 file `test_fnc_ease.rs` registered, not read), color-utility correctness (4 files in `tests/utils/`, not individually read).

The `test_full_schema_dump.rs` tests are explicitly **not** regression gates (they are `println!`-only).

<!-- <FILE>pro/EXISTING-SYSTEM-PRD/10_tests_and_verified_behaviors.md</FILE> -->
<!-- <VERS>END OF VERSION: 0.2.0</VERS> -->
